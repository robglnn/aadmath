# Standards alignment — ASCENT, Algebra I

*The Cipher Worlds · 2 units and 24 skills IN CLASS · 5 units and 62 skills on disk · Common Core and TEKS*

> **THE TWO FIGURES IN THIS DOCUMENT ARE NOT THE SAME FIGURE.** A class that opens this product today receives 2 of the 5 units and 24 of the 62 skills. That course covers **10 of the 49** Algebra I student expectations at `core`. The other 3 units are finished, validated and on disk, and no learner is sent to them; they raise the same figure to **27 of 49**. Section 1 prints both, side by side, and section 5.5 says what is holding the second one back. Any single coverage number quoted from this product without saying which of the two it is has been quoted wrongly.

| | On the shipped route | Everything on disk |
|---|---|---|
| **Units** | `algebra1-l1`, `algebra1-l2` | `algebra1-l1`, `algebra1-l2`, `algebra1-l3`, `algebra1-l4`, `algebra1-l5` |
| **Skills** | 24 | 62 |
| **How a learner reaches it** | no query string, cleared save — this is the product | `?unit=` or `?course=` typed into the address bar by hand |
| **TEKS Algebra I, cited at some depth** | **16 of 49** (32.7%) | 38 of 49 (77.6%) |
| **TEKS Algebra I, at `core`** | **10 of 49** (20.4%) | 27 of 49 (55.1%) |
| **TEKS below Algebra I** | 16 grade 6–8 expectations | 17 grade 6–8 expectations |
| **Common Core content standards** | 39 cited, 21 at `core` | 72 cited, 37 at `core` |
| **Common Core practice standards** | 8 of 8 claimed | 8 of 8 claimed |
| **True mastery, simulated — the shape a class plays** | **90.7%** playing every day, **5.3%** playing every third day, on the composed route. The 80% promise is judged on the weaker of those two and is **NOT met**. | all three cadences, in section 7.1 |
| **True mastery, simulated — one unbroken sitting** | 97.3%, and 0.0% a week later. This is the cram, it is not the shape this product ships, and no gate is judged on it. | section 7.1 |
| **TEKS authority** | 19 Texas Administrative Code, Part 2 (Texas Education Agency), Chapter 111 (Mathematics) | — |
| **TEKS adoption** | Mathematics TEKS **adopted 2012** — the version STAAR Algebra I is written against | — |
| **Citations checked** | 2026-08-19 | — |
| **Generated by** | `node tools/build-standards.mjs`. `--check` re-generates this file and byte-compares it on every run of `npm run check:standards`, and refuses to write at all if a unit has one name in a graph and another in a language bundle, or if a mastery figure below was measured against different bytes than the tree now holds. | — |

> **WHAT THE SHIPPED ROUTE IS, AND WHAT IT IS NOT.** The 10 student expectations a class receives at `core` are not spread across Algebra I. 7 of the 10 sit in **Linear functions, equations, and inequalities**, and the other 3 in Number and algebraic methods. **2 of the 4 TEKS content domains are not touched on the route at all, at any depth:** Quadratic functions and equations (8 expectations, 0 cited) and Exponential functions and equations (5 expectations, 0 cited). So the route **is** a deep, evidenced unit on the linear functions, equations, and inequalities strand, and it **is not** an Algebra I course. It cannot be adopted as one. It is adoptable as a supplement, an intervention block or a unit inside somebody else's Algebra I — which is a real thing to buy, and is not the thing the word "Algebra I" on the front of this document promises. Section 1.3 is the whole of that reading, in a table.

> **AND WHAT WOULD CHANGE IF THE PREVIEW UNITS WERE OPENED.** 3 finished units on disk carry 22 more expectations and would take the `core` figure from 10 to 27 of 49, including 3 of 8 in Quadratic functions and equations and 2 of 5 in Exponential functions and equations. They are not opened, and section 5.5 gives the measured reason in the manifest's own numbers rather than a plan: composing the next unit onto the route takes simulated true mastery from 97.3% to 64.0% on the same arm, which misses the 80% bar this product is held to. Content that exists is not a course a class can walk, and this document will not add the two together.

> **How to read this document.** Nothing below is a claim about intent. Every citation is made by a node of a knowledge graph, and every node's items are re-derived by an independent solver, rendered in strict KaTeX and produced in English, Spanish and Polish before the build will accept them. Where a claim is below `core` the alignment says which part of the expectation is not tested. Where the alignment stops, section 5 says so in writing and names the code. Where the evidence cannot be collected, section 6 says that too.

**Depth.** `core` — the expectation is the thing being taught, and the mastery gate tests it: three unassisted items at difficulty band 4 or above, at least one of them outside notation, in a form or a situation the learner has not practised. `supporting` — exercised inside items aimed at another expectation. `introduced` — a deliberately partial first encounter that a later unit completes. Every claim below `core` carries a caveat naming the part that is not tested, and `tools/validate-courses.mjs` fails the build if one does not. **A `core` claim may carry one too** — an expectation that names four sources and is taught on three is core, and the fourth is still missing. Section 3.1 lists every caveat in the course, `core` ones first, so that no depth word in this document is read without the qualification written beside it.

## 1. What a class receives, and what is on disk

The 2012 Algebra I TEKS state **49 content student expectations** across knowledge and skills statements (2) through (12) of 19 TAC §111.39(c). The seven process standards in (c)(1) are how a learner works rather than what the course teaches; they are reported separately in section 4 and are **not** counted as content coverage. Every figure below has that 49 under it.

### 1.1 The course a class receives on Monday

`content/courses.json` names the route a learner walks with no query string and a cleared save: `algebra1-l1` then `algebra1-l2`. That is 2 units and 24 skills. `npm run check:reachable` boots the shipped page with no query string, walks to a rift with real key events and proves that every unit on that list can actually be reached; the units below it cannot be, without somebody typing a query string into the address bar.

| Knowledge and skills | Expectations | Cited on the route | At `core` on the route | Cited on disk | At `core` on disk |
|---|---|---|---|---|---|
| **(2)** Linear functions, equations, and inequalities — write | 9 | 4 | 1 | 9 | 4 |
| **(3)** Linear functions, equations, and inequalities — graph | 8 | 4 | 3 | 4 | 3 |
| **(4)** Linear functions, equations, and inequalities — fit to data | 3 | 0 | 0 | 1 | 0 |
| **(5)** Linear functions, equations, and inequalities — solve | 3 | 3 | 3 | 3 | 3 |
| **(6)** Quadratic functions and equations — write | 3 | 0 | 0 | 3 | 1 |
| **(7)** Quadratic functions and equations — graph | 3 | 0 | 0 | 2 | 1 |
| **(8)** Quadratic functions and equations — solve | 2 | 0 | 0 | 1 | 1 |
| **(9)** Exponential functions and equations — write, graph and fit | 5 | 0 | 0 | 2 | 2 |
| **(10)** Number and algebraic methods — polynomials | 6 | 3 | 2 | 6 | 5 |
| **(11)** Number and algebraic methods — radicals and exponents | 2 | 0 | 0 | 2 | 2 |
| **(12)** Number and algebraic methods — equations, relations and functions | 5 | 2 | 1 | 5 | 5 |
| **Total** | **49** | **16** (32.7%) | **10** (20.4%) | 38 (77.6%) | 27 (55.1%) |

**The number to quote is the ROUTE column.** A class that is handed this product covers **10 of the 49** Algebra I student expectations at `core` — taught, and proved by the mastery gate — and 6 more in part. Those 10 are: `A.2(C)`, `A.3(A)`, `A.3(B)`, `A.3(C)`, `A.5(A)`, `A.5(B)`, `A.5(C)`, `A.10(A)`, `A.10(D)`, `A.12(E)`.

**What even that figure is not.** It is not a claim that a student who finishes the route has met 32.7% of Algebra I. It is a claim about what the content cites and what the gate tests. A `core` claim means the mastery gate demands three unassisted items at difficulty band 4 or above, at least one outside notation, in a form the learner has not practised. It does not mean the expectation has been examined by a person.

### 1.2 The rest of the course, which is on disk and switched off

3 more units — `algebra1-l3`, `algebra1-l4`, `algebra1-l5` — are written, validated and shipped in the bundle, at status `preview`. They add 38 skills. Between them they carry 22 Algebra I expectations the route does not reach at all, 14 of them at `core`, and they raise 3 more that the route reaches below `core` (`A.2(B)`, `A.10(B)`, `A.12(B)`) up to `core`. 14 + 3 = 17, which is exactly the difference between the 10 a class receives and the 27 on disk. Nothing else accounts for it.

A reader should treat those units as evidence that the content exists, and not as coverage. **Section 5.5 says what is holding them, in the manifest's own numbers.** Everything in sections 3 and 5 is marked ON ROUTE or preview so the two can never be added together by accident.

### 1.3 Which parts of Algebra I, and whether this is a course

A percentage says how much. It does not say WHICH, and for an adoption decision WHICH is the whole question. 20.4% spread evenly over Algebra I and 20.4% concentrated in one content domain are the same figure about two different products. This table is the same 49 expectations as section 1.1, grouped by the TEKS's own domain names instead of by its statement numbers.

| TEKS content domain | Statements | Expectations | Cited on the route | At `core` on the route | Cited on disk | At `core` on disk |
|---|---|---|---|---|---|---|
| **Linear functions, equations, and inequalities** | (2) (3) (4) (5) | 23 | 11 | 7 | 17 | 10 |
| **Quadratic functions and equations** | (6) (7) (8) | 8 | 0 — **none** | 0 | 6 | 3 |
| **Exponential functions and equations** | (9) | 5 | 0 — **none** | 0 | 2 | 2 |
| **Number and algebraic methods** | (10) (11) (12) | 13 | 5 | 3 | 13 | 12 |
| **Total** | 11 statements | **49** | **16** | **10** | 38 | 27 |

**Read the "on the route" columns down.** 7 of the 10 `core` claims a class receives are in **Linear functions, equations, and inequalities**, and 3 in Number and algebraic methods. **Quadratic functions and equations** and **Exponential functions and equations** are not cited at any depth on the route — 0 of 8 expectations and 0 of 5 expectations. That is the shape of this product, and it is not visible in any single percentage.

**What follows from it, in the words a district would use.**

| The question | The answer this document gives |
|---|---|
| Is this an Algebra I course? | **No.** 2 of the 4 content domains are untouched on the route. A course a district adopts for a credit has to cover the credit. |
| What is it, then? | A deep, evidenced unit on the linear functions, equations, and inequalities strand: 24 skills, 16 of 49 expectations cited on the route, 10 of them at `core`, every item re-derived by an independent solver and every claim proved by an unassisted mastery gate. |
| Can it be adopted? | **As a supplement, an intervention block, or a unit inside another Algebra I course — yes.** As the Algebra I course — no, and nothing in this document should be read as offering it as one. |
| What would close the gap? | The 3 preview units, which carry 22 more expectations and are on disk today. Section 5.5 gives the measured reason they are switched off, and it is a mastery reading and not a schedule. |

## 2. The units

| Unit | In class? | Skills | CCSS codes | CCSS `core` | TEKS codes | TEKS `core` | True mastery, every day | every third day | Working paper |
|---|---|---|---|---|---|---|---|---|---|
| `algebra1-l1` — Level 1: The Language of Balance | **ON ROUTE** | 10 | 23 | 15 | 17 | 13 | 93.0% | 66.0% — **fails its own gate** | [`algebra1-l1-standards-map.md`](standards/algebra1-l1-standards-map.md) |
| `algebra1-l2` — Level 2: Lean, Rate and Pair | **ON ROUTE** | 14 | 20 | 7 | 18 | 11 | 27.0% | 1.5% — **fails its own gate** | [`algebra1-l2-standards-map.md`](standards/algebra1-l2-standards-map.md) |
| `algebra1-l3` — Level 3: Name, Power and Form | preview — `?unit=algebra1-l3` only | 11 | 12 | 4 | 8 | 5 | 79.0% | 39.0% — **fails its own gate** | [`algebra1-l3-standards-map.md`](standards/algebra1-l3-standards-map.md) |
| `algebra1-l4` — Level 4: Root, Factor and Turn | preview — `?unit=algebra1-l4` only | 14 | 14 | 6 | 11 | 6 | 0.5% | 0.0% — **fails its own gate** | [`algebra1-l4-standards-map.md`](standards/algebra1-l4-standards-map.md) |
| `algebra1-l5` — Level 5: Rule, Fit and Pattern | preview — `?unit=algebra1-l5` only | 13 | 21 | 7 | 20 | 10 | 15.5% | 0.0% — **fails its own gate** | [`algebra1-l5-standards-map.md`](standards/algebra1-l5-standards-map.md) |

Each working paper is generated from its own graph and lists, node by node, every code, its depth, its legal citation, the caveat on any partial claim, and what the mastery gate proves. The Level 1 crosswalk, which additionally names the item forms that carry each expectation, is at [`standards/algebra1-l1-crosswalk.md`](standards/algebra1-l1-crosswalk.md).

**Read the TEKS columns with section 1.1 beside them.** The TEKS counts in this table include the grade 6–8 expectations a unit stands on, so they do not add up to the Algebra I figures above and are not meant to. Section 1.1 is the Algebra I denominator; this table is what each unit cites.

**The two mastery columns are two cadences of ONE experiment**, on that unit ALONE, from `content/standards/mastery-runs.json`: the same lattice, the same bank and the same item budget, delivered in 22-minute sittings a day apart and three days apart. Both are held to the 80% bar and the weaker one is what the gate reads. A third cadence — one unbroken sitting — is measured too and is in section 7.1, where it belongs: it flatters every unit here and a class never plays it. Section 7 carries the parameters, the lowest ability quintile, the hollow-claim rate and the composed-route figures, which are the ones about a class.

**These two columns are the HARSHEST reading in this document, and they are not the one about a class.** Each row is one unit alone, at a 900-item budget, with its cross-unit prerequisites pruned. The two route units COMPOSED, at 3600 items — which is what a class actually receives — read **90.7%** every day and **5.3%** every third day. Section 7.2 says why the two readings differ by that much, and section 7.1 is the one to quote.

## 3. Every Algebra I student expectation, one row

`—` in the depth column means no node of any unit cites it. The lines are the skills that carry the claim; a learner reaches them in prerequisite order and nothing unlocks early.

**IN CLASS** marks an expectation a learner meets on the shipped route, at the depth the route reaches. Where the route reaches it below `core` and a preview unit takes it to `core`, the row says both. A row with no **IN CLASS** mark is on disk only.

### (2) Linear functions, equations, and inequalities — write

> The student applies the mathematical process standards when using properties of linear functions to write and represent in multiple ways, with and without technology, linear equations, inequalities, and systems of equations. The student is expected to:

| Code | 19 TAC | In class? | Depth on the route | Depth on disk | Units | Lines that carry it |
|---|---|---|---|---|---|---|
| **A.2(A)** | `19 TAC §111.39(c)(2)(A)` | **IN CLASS** | supporting | supporting | l2, l3 | `compound-inequality`, `domain-range` |
| **A.2(B)** | `19 TAC §111.39(c)(2)(B)` | **IN CLASS** | supporting | core | l2, l5 | `write-linear`, `point-slope-form` |
| **A.2(C)** | `19 TAC §111.39(c)(2)(C)` | **IN CLASS** | core | core | l2, l5 | `rule-from-table`, `write-linear`, `point-slope-form` |
| **A.2(D)** | `19 TAC §111.39(c)(2)(D)` | **IN CLASS** | supporting | supporting | l2, l5 | `ratio-proportion`, `point-slope-form` |
| **A.2(E)** | `19 TAC §111.39(c)(2)(E)` | — | **not on the route** | core | l5 | `parallel-perpendicular` |
| **A.2(F)** | `19 TAC §111.39(c)(2)(F)` | — | **not on the route** | core | l5 | `parallel-perpendicular` |
| **A.2(G)** | `19 TAC §111.39(c)(2)(G)` | — | **not on the route** | supporting | l5 | `parallel-perpendicular` |
| **A.2(H)** | `19 TAC §111.39(c)(2)(H)` | — | **not on the route** | supporting | l5 | `inequality-two-var` |
| **A.2(I)** | `19 TAC §111.39(c)(2)(I)` | — | **not on the route** | supporting | l5 | `write-system` |

### (3) Linear functions, equations, and inequalities — graph

> The student applies the mathematical process standards when using graphs of linear functions, key features, and related transformations to represent in multiple ways and solve, with and without technology, equations, inequalities, and systems of equations. The student is expected to:

| Code | 19 TAC | In class? | Depth on the route | Depth on disk | Units | Lines that carry it |
|---|---|---|---|---|---|---|
| **A.3(A)** | `19 TAC §111.39(c)(3)(A)` | **IN CLASS** | core | core | l2, l5 | `slope-rate`, `point-slope-form` |
| **A.3(B)** | `19 TAC §111.39(c)(3)(B)` | **IN CLASS** | core | core | l2, l3 | `rule-from-table`, `slope-rate`, `linear-vs-exponential` |
| **A.3(C)** | `19 TAC §111.39(c)(3)(C)` | **IN CLASS** | core | core | l2 | `graph-linear` |
| **A.3(D)** | `19 TAC §111.39(c)(3)(D)` | — | **not on the route** | **not claimed** | — | — |
| **A.3(E)** | `19 TAC §111.39(c)(3)(E)` | — | **not on the route** | **not claimed** | — | — |
| **A.3(F)** | `19 TAC §111.39(c)(3)(F)` | **IN CLASS** | supporting | supporting | l2, l5 | `system-elimination`, `system-graphically` |
| **A.3(G)** | `19 TAC §111.39(c)(3)(G)` | — | **not on the route** | **not claimed** | — | — |
| **A.3(H)** | `19 TAC §111.39(c)(3)(H)` | — | **not on the route** | **not claimed** | — | — |

### (4) Linear functions, equations, and inequalities — fit to data

> The student applies the mathematical process standards to formulate statistical relationships and evaluate their reasonableness based on real-world data. The student is expected to:

| Code | 19 TAC | In class? | Depth on the route | Depth on disk | Units | Lines that carry it |
|---|---|---|---|---|---|---|
| **A.4(A)** | `19 TAC §111.39(c)(4)(A)` | — | **not on the route** | **not claimed** | — | — |
| **A.4(B)** | `19 TAC §111.39(c)(4)(B)` | — | **not on the route** | **not claimed** | — | — |
| **A.4(C)** | `19 TAC §111.39(c)(4)(C)` | — | **not on the route** | supporting | l5 | `scatter-regression`, `residual-and-fit` |

### (5) Linear functions, equations, and inequalities — solve

> The student applies the mathematical process standards to solve, with and without technology, linear equations and evaluate the reasonableness of their solutions. The student is expected to:

| Code | 19 TAC | In class? | Depth on the route | Depth on disk | Units | Lines that carry it |
|---|---|---|---|---|---|---|
| **A.5(A)** | `19 TAC §111.39(c)(5)(A)` | **IN CLASS** | core | core | l1, l2 | `one-step-add`, `one-step-mul`, `two-step`, `multi-step`, `both-sides`, `bracket-both-sides`, `fraction-solve`, `literal-equations` |
| **A.5(B)** | `19 TAC §111.39(c)(5)(B)` | **IN CLASS** | core | core | l2 | `inequality-one-step`, `inequality-two-step`, `inequality-multi-step`, `compound-inequality` |
| **A.5(C)** | `19 TAC §111.39(c)(5)(C)` | **IN CLASS** | core | core | l2, l5 | `system-substitution`, `system-elimination`, `system-graphically` |

### (6) Quadratic functions and equations — write

> The student applies the mathematical process standards when using properties of quadratic functions to write and represent in multiple ways, with and without technology, quadratic equations. The student is expected to:

| Code | 19 TAC | In class? | Depth on the route | Depth on disk | Units | Lines that carry it |
|---|---|---|---|---|---|---|
| **A.6(A)** | `19 TAC §111.39(c)(6)(A)` | — | **not on the route** | supporting | l4 | `parabola-features` |
| **A.6(B)** | `19 TAC §111.39(c)(6)(B)` | — | **not on the route** | core | l4 | `complete-the-square`, `quadratic-from-vertex` |
| **A.6(C)** | `19 TAC §111.39(c)(6)(C)` | — | **not on the route** | supporting | l4 | `quadratic-from-vertex`, `quadratic-model` |

### (7) Quadratic functions and equations — graph

> The student applies the mathematical process standards when using graphs of quadratic functions and their related transformations to represent in multiple ways and determine, with and without technology, the solutions to equations. The student is expected to:

| Code | 19 TAC | In class? | Depth on the route | Depth on disk | Units | Lines that carry it |
|---|---|---|---|---|---|---|
| **A.7(A)** | `19 TAC §111.39(c)(7)(A)` | — | **not on the route** | supporting | l4 | `parabola-features`, `quadratic-model` |
| **A.7(B)** | `19 TAC §111.39(c)(7)(B)` | — | **not on the route** | core | l4 | `quadratic-zero-product` |
| **A.7(C)** | `19 TAC §111.39(c)(7)(C)` | — | **not on the route** | **not claimed** | — | — |

### (8) Quadratic functions and equations — solve

> The student applies the mathematical process standards to solve, with and without technology, quadratic equations and evaluate the reasonableness of their solutions. The student is expected to:

| Code | 19 TAC | In class? | Depth on the route | Depth on disk | Units | Lines that carry it |
|---|---|---|---|---|---|---|
| **A.8(A)** | `19 TAC §111.39(c)(8)(A)` | — | **not on the route** | core | l4 | `quadratic-zero-product`, `solve-by-factoring`, `square-root-method`, `complete-the-square`, `quadratic-formula` |
| **A.8(B)** | `19 TAC §111.39(c)(8)(B)` | — | **not on the route** | **not claimed** | — | — |

### (9) Exponential functions and equations — write, graph and fit

> The student applies the mathematical process standards when using properties of exponential functions and their related transformations to write, graph, and represent in multiple ways exponential equations and evaluate, with and without technology, the reasonableness of their solutions. The student formulates statistical relationships and evaluates their reasonableness based on real-world data. The student is expected to:

| Code | 19 TAC | In class? | Depth on the route | Depth on disk | Units | Lines that carry it |
|---|---|---|---|---|---|---|
| **A.9(A)** | `19 TAC §111.39(c)(9)(A)` | — | **not on the route** | **not claimed** | — | — |
| **A.9(B)** | `19 TAC §111.39(c)(9)(B)` | — | **not on the route** | core | l3, l5 | `linear-vs-exponential`, `exponential-rule`, `exponential-model` |
| **A.9(C)** | `19 TAC §111.39(c)(9)(C)` | — | **not on the route** | core | l5 | `exponential-model` |
| **A.9(D)** | `19 TAC §111.39(c)(9)(D)` | — | **not on the route** | **not claimed** | — | — |
| **A.9(E)** | `19 TAC §111.39(c)(9)(E)` | — | **not on the route** | **not claimed** | — | — |

### (10) Number and algebraic methods — polynomials

> The student applies the mathematical process standards and algebraic methods to rewrite in equivalent forms and perform operations on polynomial expressions. The student is expected to:

| Code | 19 TAC | In class? | Depth on the route | Depth on disk | Units | Lines that carry it |
|---|---|---|---|---|---|---|
| **A.10(A)** | `19 TAC §111.39(c)(10)(A)` | **IN CLASS** | core | core | l1, l3 | `like-terms`, `poly-add-sub` |
| **A.10(B)** | `19 TAC §111.39(c)(10)(B)` | **IN CLASS** | supporting | core | l1, l3 | `distribute`, `poly-multiply` |
| **A.10(C)** | `19 TAC §111.39(c)(10)(C)` | — | **not on the route** | core | l4 | `poly-divide` |
| **A.10(D)** | `19 TAC §111.39(c)(10)(D)` | **IN CLASS** | core | core | l1, l3, l4 | `distribute`, `multi-step`, `poly-multiply`, `factor-common`, `factor-trinomial-monic` |
| **A.10(E)** | `19 TAC §111.39(c)(10)(E)` | — | **not on the route** | core | l4 | `factor-trinomial-monic`, `factor-trinomial-lead` |
| **A.10(F)** | `19 TAC §111.39(c)(10)(F)` | — | **not on the route** | supporting | l4 | `difference-of-squares` |

### (11) Number and algebraic methods — radicals and exponents

> The student applies the mathematical process standards and algebraic methods to rewrite algebraic expressions into equivalent forms. The student is expected to:

| Code | 19 TAC | In class? | Depth on the route | Depth on disk | Units | Lines that carry it |
|---|---|---|---|---|---|---|
| **A.11(A)** | `19 TAC §111.39(c)(11)(A)` | — | **not on the route** | core | l4, l5 | `radical-simplify`, `radical-arith`, `square-root-method`, `rational-exponent` |
| **A.11(B)** | `19 TAC §111.39(c)(11)(B)` | — | **not on the route** | core | l3, l5 | `exponent-product`, `exponent-power`, `exponent-quotient`, `zero-negative-exponent`, `exponential-rule`, `rational-exponent` |

### (12) Number and algebraic methods — equations, relations and functions

> The student applies the mathematical process standards and algebraic methods to write, solve, analyze, and evaluate equations, relations, and functions. The student is expected to:

| Code | 19 TAC | In class? | Depth on the route | Depth on disk | Units | Lines that carry it |
|---|---|---|---|---|---|---|
| **A.12(A)** | `19 TAC §111.39(c)(12)(A)` | — | **not on the route** | core | l5 | `relation-is-function` |
| **A.12(B)** | `19 TAC §111.39(c)(12)(B)` | **IN CLASS** | introduced | core | l1, l3 | `var-meaning`, `eval-expr`, `function-notation`, `exponential-rule` |
| **A.12(C)** | `19 TAC §111.39(c)(12)(C)` | — | **not on the route** | core | l5 | `sequence-terms` |
| **A.12(D)** | `19 TAC §111.39(c)(12)(D)` | — | **not on the route** | core | l5 | `sequence-nth-term` |
| **A.12(E)** | `19 TAC §111.39(c)(12)(E)` | **IN CLASS** | core | core | l2 | `literal-equations` |

### 3.1 Every claim that is not the whole expectation

139 of the claims in this document cover part of the expectation they cite and say so. 2 of those are marked `core` **on the shipped route**: the expectation is the thing being taught and the mastery gate tests it, and one clause of it is still not reached. Every caveat below is written on the graph node that makes the claim, is checked by `tools/validate-courses.mjs`, and is printed to a learner and to a teacher in their own language. This table is here because a one-word depth column cannot carry any of it.

| Code | In class? | Depth | Line | What is NOT tested |
|---|---|---|---|---|
| **A.2(C)** | **IN CLASS** | **core** | `write-linear` | Two of the three sources are tested: wl-table writes the rule from a table of values and wl-graph writes it from a graph. The verbal description is NOT. wl-context prints a sentence that carries no number, beside the same two readings wl-points prints, so the learner works from the readings and the sentence only sets the scene. |
| **A.3(A)** | **IN CLASS** | **core** | `slope-rate` | This node finds the slope from a table of values, a graph and two points. Of the three equation forms the expectation names, only Ax + By = C appears here. The y = mx + b form belongs to graph-linear and write-linear, and the point-slope form y - y1 = m(x - x1) is not in this unit. |
| **7.11(A)** | **IN CLASS** | supporting | `inequality-multi-step` | The two-step case is the floor of this node, not its target. The expectation stops at two steps and never reaches a bracket or an unknown on both sides. |
| **8.4(A)** | **IN CLASS** | supporting | `slope-rate` | The learner uses the fact that the rate is the same between any two points and meets it on tables, points and traces. The similar-triangles argument for why it is the same is not made. |
| **8.5(B)** | **IN CLASS** | supporting | `graph-linear` | Tables and graphs are both used and the rule is read in y = mx + b form. Writing that equation down is write-linear, which claims the expectation as core. |
| **8.9(A)** | **IN CLASS** | supporting | `system-substitution` | Verifying a pair against both statements is tested directly. Reading the pair off an intersection on a drawn graph is offered but is not what the gate proves. |
| **A.2(A)** | **IN CLASS** | supporting | `compound-inequality` | Only the last clause is claimed: representing a set of allowed values using inequalities. Domain and range of a function are not treated in Level 2. |
| **A.2(B)** | **IN CLASS** | supporting | `write-linear` | The two-points case is core here, but only the y = mx + b form is written. The standard form and the point-slope form are not produced. |
| **A.2(C)** | **IN CLASS** | supporting | `rule-from-table` | The learner reads the rule and uses it. Writing the equation down in y = mx + b form is Level 3. |
| **A.2(D)** | **IN CLASS** | supporting | `ratio-proportion` | The proportional relationship is written and solved, but it is never named as direct variation and the constant is never written as k. |
| **A.3(B)** | **IN CLASS** | supporting | `rule-from-table` | No question on this line asks for the rate. All four forms are answered with a value out of the table, so the rate is a step the learner takes and never an answer the rig reads. Calculating the rate is core at slope-rate. |
| **A.3(F)** | **IN CLASS** | supporting | `system-elimination` | The solution of a pair is determined and is the point where two traces would meet. This node does not ask the learner to draw the two traces first. |
| **A.5(A)** | **IN CLASS** | supporting | `fraction-solve` | The rational-coefficient case is inside A.5(A) but is not one of the two cases the expectation names, so the claim is supporting. |
| **A.5(A)** | **IN CLASS** | supporting | `literal-equations` | The moves are the moves of A.5(A), but the statement being solved carries letters and not numbers, so the claim is supporting rather than core. The distributive case A.5(A) names by name is not here either: no statement in this node carries a bracket to open. It is tested at bracket-both-sides, which claims A.5(A) as core. |
| **A.5(B)** | **IN CLASS** | supporting | `inequality-one-step` | A.5(B) names two hard cases: the distributive property, and variables on both sides. This node tests neither. Both arrive at inequality-multi-step. |
| **A.5(B)** | **IN CLASS** | supporting | `inequality-two-step` | The two cases A.5(B) names by name are still absent. They are tested at inequality-multi-step, which claims A.5(B) as core. |
| **A.5(B)** | **IN CLASS** | supporting | `compound-inequality` | A.5(B) names two cases: the distributive property, and variables on both sides. The four forms here test neither. Every statement is one band, with no brackets, and the unknown stands on one side only. This node adds a band held at both ends, and a negative coefficient that turns it. Both named cases are tested at inequality-multi-step, which claims A.5(B) as core. |
| **6.EE.B.8** | **IN CLASS** | supporting | `inequality-one-step` | The learner reads and chooses statements of the form x > c. Number-line diagrams are not drawn anywhere in this unit. |
| **6.RP.A.3** | **IN CLASS** | supporting | `ratio-proportion` | Tables of equivalent ratios and equations are tested. Tape diagrams and double number lines are not drawn anywhere in this unit. |
| **7.EE.B.4.A** | **IN CLASS** | supporting | `fraction-solve` | Covered in the (x + q)/p framing, which is the same equation written with the division shown rather than a bracket. |
| **7.EE.B.4.B** | **IN CLASS** | supporting | `inequality-two-step` | The word-problem and solving halves are core here. Graphing the solution set is not: this unit never draws a solution set on a number line. |
| **8.EE.C.8.B** | **IN CLASS** | supporting | `system-substitution` | Solved algebraically and read off a drawn pair of traces. Estimating a solution by graphing is not asked, because every pair here meets at whole numbers. |
| **8.EE.C.8.C** | **IN CLASS** | supporting | `system-elimination` | The situations lead to two linear equations in two variables and are solved. The expectation also expects the learner to write the pair from scratch, which system-substitution and write-linear cover. |
| **HSA.CED.A.1** | **IN CLASS** | supporting | `inequality-multi-step` | Linear only. Quadratic, rational and exponential equations are named by the expectation and are outside Algebra I Level 2. |
| **HSA.CED.A.3** | **IN CLASS** | supporting | `compound-inequality` | Constraints are represented and read back as a band. Systems of inequalities, and judging an option viable or nonviable, are not tested. |
| **HSA.REI.B.3** | **IN CLASS** | supporting | `literal-equations` | This node meets the clause "including equations with coefficients represented by letters". The numeric-solution half of REI.B.3 belongs to Level 1 and to the equation nodes here. |
| **HSA.REI.D.10** | **IN CLASS** | supporting | `graph-linear` | Lines only. The learner never plots a curve, so the general statement about equations in two variables is met in its linear case alone. |
| **HSA.SSE.A.1.B** | **IN CLASS** | supporting | `bracket-both-sides` | The learner has to read a(x + b) as one quantity before opening it. The full expectation also covers non-linear structure, which this unit does not reach. |
| **HSF.IF.B.6** | **IN CLASS** | supporting | `slope-rate` | Linear only, so the average rate of change over an interval is the rate of the whole rule. Non-linear functions, where the two differ, are not reached. |
| **HSF.IF.C.7.A** | **IN CLASS** | supporting | `graph-linear` | Linear functions only, and intercepts only. Quadratic functions, maxima and minima are named by the expectation and are outside Algebra I Level 2. |
| **HSF.LE.A.2** | **IN CLASS** | supporting | `rule-from-table` | Linear only. Nothing in this unit constructs an exponential function. |
| **HSF.LE.A.2** | **IN CLASS** | supporting | `write-linear` | Linear only. Nothing in this unit constructs an exponential function or a geometric sequence. |
| **A.10(E)** | preview | **core** | `factor-trinomial-lead` | Every clause that names a value is proved: the squared term carries a number, and a perfect square trinomial of degree two is one of the printed forms. The words "if possible" are not asked, because answering that a trinomial does not factor is a verdict this unit cannot print in three languages. |
| **A.11(A)** | preview | **core** | `radical-arith` | This node joins, opens and clears radicals. Reducing one radical to its shortest form is proved by the node before it, and this gate assumes it rather than re-testing it. |
| **A.11(B)** | preview | **core** | `exponent-product` | The laws are applied to whole-number and negative whole-number counts throughout. The rational-exponent half of this expectation is not taught in this unit and is not claimed. |
| **A.11(B)** | preview | **core** | `exponent-power` | The laws are applied to whole-number and negative whole-number counts throughout. The rational-exponent half of this expectation is not taught in this unit and is not claimed. |
| **A.11(B)** | preview | **core** | `exponent-quotient` | The laws are applied to whole-number and negative whole-number counts throughout. The rational-exponent half of this expectation is not taught in this unit and is not claimed. |
| **A.11(B)** | preview | **core** | `zero-negative-exponent` | The laws are applied to whole-number and negative whole-number counts throughout. The rational-exponent half of this expectation is not taught in this unit and is not claimed. |
| **A.11(B)** | preview | **core** | `rational-exponent` | The integral half of this expectation is not asked here. Every count on screen is a fraction, or a square root written as one. Whole-number and negative whole-number counts are the prerequisite zero-negative-exponent, and Level 3 claims them at core. This node adds the rational half. |
| **A.6(B)** | preview | **core** | `quadratic-from-vertex` | Two of the three clauses are proved with the turning point given: the rule is written in vertex form, and a rule in vertex form is rewritten to standard form. Building a rule from a turning point and a second reading ALONE is not asked, because such a prompt shows no rule for the checker to read. |
| **A.7(B)** | preview | **core** | `quadratic-zero-product` | The expectation says "describe". No sentence is asked for. The relationship is used in both directions and every reading is proved by putting it back into the printed statement. |
| **A.8(A)** | preview | **core** | `solve-by-factoring` | This node proves the factoring route. Taking square roots, completing the square and the formula are proved by the three nodes after it, and the expectation closes only when all four are held. |
| **A.8(A)** | preview | **core** | `square-root-method` | This node proves the taking-square-roots route, including the case where no real answer exists. Factoring, completing the square and the formula are proved by the nodes around it. |
| **A.8(A)** | preview | **core** | `complete-the-square` | This node proves the completing-the-square route, including the case where the squared term carries a number that has to come out first. The other three methods are proved by the nodes around it. |
| **A.8(A)** | preview | **core** | `quadratic-formula` | This node proves the formula route, including the reading of the part under the root that says there is no real answer. The other three methods are proved by the nodes before it. |
| **HSA.APR.A.1** | preview | **core** | `poly-add-sub` | Addition and subtraction are what the gate tests here. The multiplication half of this standard, and the closure argument the standard opens with, belong to the multiply-a-binomial node. |
| **HSA.APR.A.1** | preview | **core** | `poly-multiply` | Multiplication is what the gate tests here. The addition and subtraction half of this standard belongs to the join-two-polynomials node, and the closure argument is stated in neither. |
| **HSA.REI.B.4.B** | preview | **core** | `solve-by-factoring` | The factoring route is proved, and so is the rearranging that has to come first. The complex-solution clause is outside this course: no item prints a number that is not real. |
| **HSA.REI.B.4.B** | preview | **core** | `square-root-method` | Solving by inspection and by taking square roots is proved, on both the plain and the shifted form. The complex-solution clause is outside this course. |
| **HSA.REI.B.4.B** | preview | **core** | `quadratic-formula` | The formula route is proved, on rational and on irrational answers alike. The complex-solution clause is outside this course: an equation whose part under the root is below zero is answered "no solution". |
| **HSA.SSE.A.2** | preview | **core** | `factor-trinomial-monic` | The rewriting is proved twice over: the answer is the same function at fourteen values AND it is a product. The standard also covers structures this unit never prints, such as a difference of two cubes. |
| **HSA.SSE.A.2** | preview | **core** | `factor-trinomial-lead` | The rewriting is proved by identity and by shape. The standard also covers structures this unit never prints. |
| **HSA.SSE.A.2** | preview | **core** | `difference-of-squares` | Seeing a binomial as one square take away another is exactly using structure to rewrite. The standard also covers structures this unit never prints. |
| **HSA.SSE.B.3.A** | preview | **core** | `quadratic-zero-product` | The zeros are revealed by the factors, in both directions. Producing the factors from a three-term expression is the factoring nodes’ gate rather than this one. |
| **HSA.SSE.B.3.B** | preview | **core** | `complete-the-square` | The square is completed and the value at the turn appears in the answer. Naming which of maximum and minimum it is belongs to the features node. |
| **HSF.IF.A.2** | preview | **core** | `function-notation` | Using the notation and evaluating at an element of the domain are core and are what the gate tests. Interpreting a statement in function notation in terms of a context is met only by the situation forms, where the rule is named by a machine and read at one setting. |
| **HSF.IF.A.2** | preview | **core** | `exponential-rule` | Using the notation and evaluating at an element of the domain are core and are what the gate tests. Interpreting a statement in function notation in terms of a context is met only by the situation forms, where the rule is named by a machine and read at one setting. |
| **HSF.IF.B.4** | preview | **core** | `quadratic-model` | The key features of a printed rule and of a table of readings are interpreted in the quantities of the situation. No graph is sketched, and the standard names sketching as well. |
| **HSF.IF.C.8.A** | preview | **core** | `parabola-features` | Factoring and completing the square are what produce the zeros, the value at the turn and the line of symmetry, and one form asks for them inside a situation. The graph itself is not drawn. |
| **8.11(A)** | preview | supporting | `association-strength` | The observed data are described to address a question of association, which is the second half. CONSTRUCTING a scatterplot is the first half and is not met: the bivariate readings arrive as a printed table. |
| **A.10(D)** | preview | supporting | `factor-trinomial-monic` | Level 3 proves the forward direction of this expectation. This node runs it backwards, and the gate marks the product rather than the naming of the property. |
| **A.10(E)** | preview | supporting | `factor-trinomial-monic` | Only trinomials whose squared term carries a one are factored here. A squared term with a number in front is the next node, and the words "if possible" are never asked. |
| **A.10(F)** | preview | supporting | `difference-of-squares` | The rewriting half is proved: the answer must be the same expression at fourteen values and must be a product of two brackets. The deciding half is asked as a choice between two printed binomials, and the checker proves only the one the learner chose; it does not re-derive the refusal of the other. |
| **A.11(A)** | preview | supporting | `square-root-method` | A radical arrives from a root taken of both sides and is reduced before the answer is accepted. The gate here marks the solution set, not the reduction. |
| **A.11(A)** | preview | supporting | `rational-exponent` | Simplifying a numerical square root is Level 4's CORE claim, on its own radical nodes, and this node does not restate it. What is claimed here is the bridge: the same simplification carried out under a fractional count, so a learner reads a count of one half and a square root as one idea rather than two. A radical whose radicand is not a whole number, and a sum of two unlike radicals, both belong to Level 4 and are not served here. |
| **A.11(B)** | preview | supporting | `exponential-rule` | The laws are applied to whole-number and negative whole-number counts throughout. The rational-exponent half of this expectation is not taught in this unit and is not claimed. |
| **A.2(A)** | preview | supporting | `domain-range` | Only the first clause is met, and only for a discrete domain: the domain is a listed run of whole numbers and the learner works out the output at an end of it. This unit does NOT represent domain or range using inequalities, and does not treat a continuous domain. |
| **A.2(C)** | preview | supporting | `point-slope-form` | A linear equation is written from a table of values and from a verbal description of a steady rate. It is NOT written from a graph, because this unit draws none. |
| **A.2(D)** | preview | supporting | `point-slope-form` | A direct-variation rule is WRITTEN from a table whose readings pass through zero, and the constant is worked out by dividing an output by its input. Solving an equation that involves direct variation is not asked for here; Level 2 carries that half. |
| **A.2(G)** | preview | supporting | `parallel-perpendicular` | A line beside an upright line, and a line at a right angle to one, are both WRITTEN — x = a and y = b — and the checker refuses an answer that does not pass through the printed reading. Reporting in words that the rate is zero or undefined is NOT asked for: a verdict of that shape would have to appear on screen as an English token, because `finalize` localises only two verdict words, and an English token on a Polish screen is not something this unit ships. |
| **A.2(H)** | preview | supporting | `inequality-two-var` | A linear inequality in two variables is written from a table of values and from a verbal description. It is NOT written from a graph: this unit shades no region and draws no boundary. The table carries the boundary readings, one mark saying whether the boundary belongs to the region, and one reading marked inside or outside. |
| **A.2(I)** | preview | supporting | `write-system` | A pair of linear equations is written from two tables of values and from a verbal description. It is NOT written from a graph, because this unit draws none. |
| **A.3(A)** | preview | supporting | `point-slope-form` | The rate is read off an equation in all three written forms, and off a table of values. It is not read off a graph, and it is not read off two plotted points, because this unit plots nothing. |
| **A.3(B)** | preview | supporting | `linear-vs-exponential` | The rate is calculated tabularly and algebraically, from a steady log and from a difference of two readings. It is not calculated graphically, because this unit draws no graph; that half is held by the slope-and-rate node of Level 2. |
| **A.3(F)** | preview | supporting | `system-graphically` | The second half — "determine the solutions if they exist" — is core and is what the gate tests, through a checker that solves the printed pair by Cramer's rule in exact rationals and refuses a pair that does not meet at one point. GRAPHING the system on the coordinate plane is not met: each rule's own readings are printed in a table beside it, and the crossing is the row that appears in both, which is a table and not a plane. |
| **A.4(C)** | preview | supporting | `scatter-regression` | This claim is supporting, not core. A.4(C) asks the learner to WRITE a linear function. No question here asks for one: all five forms take a single number as the answer, which is the rate, the starting value or one prediction. The prediction half is proved, because a value is estimated past the end of the readings in a real situation. The writing half is not. |
| **A.4(C)** | preview | supporting | `residual-and-fit` | The prediction half of the expectation is served, and the residual is what says whether the prediction is worth making. WRITING the linear function is the previous node's core claim, not this one's. |
| **A.6(A)** | preview | supporting | `parabola-features` | The range is produced as an inequality and proved by an identity that reproduces the printed rule. The domain is not asked: every rule of degree two accepts every real number, so the answer would be recall rather than a derivation. |
| **A.6(B)** | preview | supporting | `complete-the-square` | Vertex form is produced from standard form and proved. Writing a rule from a turning point and a second reading is the later node, and it is not asked here. |
| **A.6(C)** | preview | supporting | `quadratic-from-vertex` | The learner is given the real solutions and writes the rule as a product of two brackets. The graphs half of the expectation is not met, because no curve is drawn. |
| **A.6(C)** | preview | supporting | `quadratic-model` | A squared rule is read inside a situation, and its greatest value, its best input and the time it reaches zero are produced. Writing the rule from stated solutions is the node before this one, and no graph is drawn. |
| **A.7(A)** | preview | supporting | `parabola-features` | Every attribute the expectation names is produced from the printed rule and re-derived exactly: both crossings, the zeros, the greatest or least value, the turning point and the equation of the line of symmetry. The graphing clause is NOT met, because the renderer draws straight segments and isolated points only and cannot draw a curve. |
| **A.7(A)** | preview | supporting | `quadratic-model` | The key attributes are produced inside real-world problems, which is what the expectation asks them to be used for. No graph is drawn. |
| **A.8(A)** | preview | supporting | `quadratic-zero-product` | Only a product that already reads zero is solved here. The four methods the expectation names belong to the four nodes after this one. |
| **A.9(B)** | preview | supporting | `linear-vs-exponential` | The learner produces the value of a, by reading the rule at zero, and the value of b, by dividing one reading by the one before it. The learner is not asked to write a sentence explaining what either value means in the situation, so the claim is supporting rather than core. |
| **A.9(B)** | preview | supporting | `exponential-rule` | The learner produces the value of a, by reading the rule at zero, and the value of b, by dividing one reading by the one before it. The learner is not asked to write a sentence explaining what either value means in the situation, so the claim is supporting rather than core. |
| **6.EE.A.1** | preview | supporting | `exponent-product` | Whole-number counts are written and evaluated. Prime factorization, which this expectation also names, is not taught in this unit and is not claimed. |
| **8.EE.A.1** | preview | supporting | `exponent-product` | This node rewrites expressions whose base is a letter. The expectation names numerical expressions, and that half is carried by the zero-and-negative-count node, whose numeric form works out an exact value. |
| **8.EE.A.1** | preview | supporting | `exponent-quotient` | This node rewrites expressions whose base is a letter. The expectation names numerical expressions, and that half is carried by the zero-and-negative-count node, whose numeric form works out an exact value. |
| **HSA.APR.B.3** | preview | supporting | `quadratic-zero-product` | The zeros are identified from a factorisation that is already available. No rough graph is drawn: the renderer draws no curve. |
| **HSA.APR.D.6** | preview | supporting | `poly-divide` | The division is done and the leftover is named. The traditional pathway places this standard in Algebra II, and this node divides only by expressions of degree one and degree two, with a leftover of degree zero. |
| **HSA.CED.A.1** | preview | supporting | `solve-by-factoring` | One form builds the equation from a stated area before solving it. Most forms hand the learner the equation already written. |
| **HSA.CED.A.1** | preview | supporting | `quadratic-formula` | One form builds the equation from a thrown object before solving it. Most forms hand the learner the equation already written. |
| **HSA.CED.A.1** | preview | supporting | `quadratic-model` | The landing forms build an equation from the story and solve it. The reading forms are handed the rule already written. |
| **HSA.CED.A.2** | preview | supporting | `quadratic-from-vertex` | A rule in two quantities is written, in two spellings. It is never graphed with labels and scales, because the renderer draws no curve. |
| **HSA.CED.A.2** | preview | supporting | `point-slope-form` | Equations in two variables are created to represent a relationship, which is the first clause. Graphing them on coordinate axes with labels and scales is the second clause and is not met: this unit draws no axes. |
| **HSA.CED.A.2** | preview | supporting | `parallel-perpendicular` | The equation of the new line is created from a printed line and a printed reading, which is the first clause. Graphing it on coordinate axes is the second clause and is not met. |
| **HSA.CED.A.2** | preview | supporting | `write-system` | Two equations in two variables are created from two data sets, which is the first clause. Graphing them on coordinate axes is the second clause and is not met. |
| **HSA.CED.A.3** | preview | supporting | `inequality-two-var` | A constraint is represented by an inequality in two variables. Representing one by a SYSTEM of inequalities, and interpreting a solution as viable or nonviable in a modelling context, are not tested. |
| **HSA.CED.A.3** | preview | supporting | `write-system` | Two constraints are represented by a system of equations, which is the middle clause. Representing a constraint by an inequality belongs to the previous node, and interpreting a solution as viable or nonviable in a modelling context is not tested. |
| **HSA.REI.B.4.A** | preview | supporting | `complete-the-square` | The transformation into a squared bracket equal to a number is produced and proved. Deriving the formula from that form is stated in the next node and is never asked of the learner. |
| **HSA.REI.C.6** | preview | supporting | `system-graphically` | Solving a pair of linear equations EXACTLY is core and is what the checker does. Solving one approximately, which the expectation illustrates with graphs, is not met: this unit draws nothing and reports no approximate value. |
| **HSA.REI.D.11** | preview | supporting | `system-graphically` | The crossing of two rules really is where one equals the other, and the table of values route the expectation names is the route this node takes. The EXPLANATION of why, and the cases where either rule is polynomial, rational, absolute value, exponential or logarithmic, are not tested: both rules here are straight. |
| **HSA.REI.D.12** | preview | supporting | `inequality-two-var` | The half-plane is NAMED rather than graphed, and the strictness clause — "excluding the boundary in the case of a strict inequality" — is exactly what the boundary mark tests. Graphing the region, and intersecting two of them, are not met. |
| **HSA.SSE.A.1.A** | preview | supporting | `poly-add-sub` | The parts a learner has to read here are terms and coefficients. Reading an expression's factors is the factoring node's job, and interpreting a part of a context is not asked for. |
| **HSA.SSE.A.1.A** | preview | supporting | `factor-common` | The parts a learner has to read here are terms and coefficients. Reading an expression's factors is the factoring node's job, and interpreting a part of a context is not asked for. |
| **HSA.SSE.A.1.A** | preview | supporting | `factor-trinomial-monic` | The pair of numbers is chosen by reading the last term as a product and the middle term as a sum. The standard also names coefficients in expressions this node does not reach. |
| **HSA.SSE.A.1.B** | preview | supporting | `poly-multiply` | A bracket has to be read as one quantity before it is opened or squared. The full expectation also covers non-polynomial structure, which this unit does not reach. |
| **HSA.SSE.A.1.B** | preview | supporting | `radical-arith` | A root has to be read as one quantity before it can be counted or multiplied. The full expectation also covers expressions this unit never prints. |
| **HSA.SSE.A.1.B** | preview | supporting | `factor-trinomial-lead` | A bracket carrying both a number and a letter has to be read as one quantity. The full expectation also covers non-polynomial structure. |
| **HSA.SSE.A.2** | preview | supporting | `exponent-product` | The structure used here is a product, a quotient or a common factor. The full expectation also covers structures this unit never reaches, including the difference of two squares and the quadratic trinomial. |
| **HSA.SSE.A.2** | preview | supporting | `exponent-power` | The structure used here is a product, a quotient or a common factor. The full expectation also covers structures this unit never reaches, including the difference of two squares and the quadratic trinomial. |
| **HSA.SSE.A.2** | preview | supporting | `exponent-quotient` | The structure used here is a product, a quotient or a common factor. The full expectation also covers structures this unit never reaches, including the difference of two squares and the quadratic trinomial. |
| **HSA.SSE.A.2** | preview | supporting | `zero-negative-exponent` | The structure used here is a product, a quotient or a common factor. The full expectation also covers structures this unit never reaches, including the difference of two squares and the quadratic trinomial. |
| **HSA.SSE.A.2** | preview | supporting | `radical-simplify` | The structure read here sits inside a whole number under a bar, not inside an algebraic expression. The polynomial half of this standard is carried by the factoring nodes. |
| **HSA.SSE.A.2** | preview | supporting | `radical-arith` | Two roots that share a number under the bar are read as like terms, which is structure. The standard also covers polynomial structure, which the factoring nodes carry. |
| **HSA.SSE.A.2** | preview | supporting | `poly-divide` | The top is rewritten as a product before the division is read off. The standard also covers rewritings this node does not perform. |
| **HSA.SSE.B.3.A** | preview | supporting | `quadratic-from-vertex` | A rule is written as a product whose brackets are its zeros, in both directions. Revealing the zeros of an unfactored expression is the factoring nodes’ gate. |
| **HSF.BF.A.1.A** | preview | supporting | `sequence-terms` | A recursive process is determined from a printed list and from a printed recursion, and run forward to a named position. Determining one FROM A CONTEXT is met only by the situation forms, where the story states the step and the table carries it; the learner is never asked to invent the process from prose alone. |
| **HSF.IF.A.1** | preview | supporting | `function-notation` | What this node meets is the notation clause: f is a name, x is an element of its domain, and f(x) is the output. The unit never asks a learner to decide whether a relation assigns exactly one output to each input, because the independent checker cannot re-derive that judgement. See the unit's declared gaps. |
| **HSF.IF.A.1** | preview | supporting | `domain-range` | What this node meets is the notation clause: f is a name, x is an element of its domain, and f(x) is the output. The unit never asks a learner to decide whether a relation assigns exactly one output to each input, because the independent checker cannot re-derive that judgement. See the unit's declared gaps. |
| **HSF.IF.B.4** | preview | supporting | `parabola-features` | Key features are read off a printed rule and off a table of readings. No graph is read and none is sketched, and the standard names both. |
| **HSF.IF.B.5** | preview | supporting | `domain-range` | The domain is related to the quantitative relationship it describes: a listed run of whole inputs, and the outputs the rule gives at its ends. It is NOT related to a graph, because this unit draws none. |
| **HSF.IF.C.7.A** | preview | supporting | `parabola-features` | The intercepts, the maximum and the minimum are produced exactly. No graph is drawn, and the standard asks for one. |
| **HSF.LE.A.1.A** | preview | supporting | `linear-vs-exponential` | The learner produces both numbers the standard names — the common difference off a difference of two readings, and the common factor off a quotient of two readings — and watches the factor overtake the difference. The learner is not asked to prove the general statement, so the claim is supporting. |
| **HSF.LE.A.1.B** | preview | supporting | `linear-vs-exponential` | The constant-rate half is met by the steady-log table and by the difference form, where the learner produces the amount added at every step. The situations are the unit's own, and the learner recognises the constant rate inside them rather than choosing between candidate situations. |
| **HSF.LE.A.2** | preview | supporting | `sequence-nth-term` | Arithmetic and geometric sequences are constructed from a table of positions and values, which the expectation names outright. Constructing one from a GRAPH is not tested, because this unit draws none, and constructing an exponential function from a description belongs to the exponential-model node. |
| **HSF.LE.A.2** | preview | supporting | `exponential-model` | An exponential function is constructed from a table of input-output pairs, which the expectation names outright. Constructing one from a GRAPH is not tested, and the linear half of this expectation belongs to Level 2. |
| **HSF.LE.A.3** | preview | supporting | `exponential-model` | A quantity that multiplies is observed to overtake one that adds, USING TABLES: the checker walks both printed rules over a stated run of whole positions and reports the first position where the multiplying rule leads. The graph half is not met, and neither is the quadratic or polynomial comparison — the rule that is overtaken here is always straight. |
| **HSN.RN.A.1** | preview | supporting | `rational-exponent` | Rewriting a radical as a rational exponent, and back, is core and is what the gate tests. EXPLAINING why the definition follows from the laws of integer exponents is not asked for: the bank marks a value, not an argument, and this unit ships no item whose answer is a sentence. |
| **HSS.ID.B.6** | preview | supporting | `scatter-regression` | How the two measurements are related is described, through the exact rate of the fitted line. Representing them ON A SCATTER PLOT is not met. |
| **HSS.ID.B.6** | preview | supporting | `association-strength` | How two measurements are related is described by the exact sign of the fitted rate: above zero they rise together, below zero one falls as the other rises. Representing them ON A SCATTER PLOT is not met, and neither is any statement about how STRONG the association is — see the unit's declared gaps. |
| **HSS.ID.B.6.A** | preview | supporting | `scatter-regression` | A function is fitted to the data and used to solve problems in the context of the data, which is the first half. The emphasis on quadratic and exponential models is not met here: every model in this node is linear, and the exponential half lives in the exponential-model node. |
| **HSS.ID.B.6.A** | preview | supporting | `residual-and-fit` | A fitted function is used to solve problems in the context of the data, and the residuals say whether the function suggested by the context was the right one. The emphasis on quadratic and exponential models is not met: every model here is linear. |
| **HSS.ID.B.6.B** | preview | supporting | `residual-and-fit` | Residuals are ANALYSED: each one is the reading take away the fitted value, in that order, and a run of gaps that keeps one sign is printed back as the reason a straight rule is the wrong shape. PLOTTING them is not met, because this unit draws nothing. |
| **HSS.ID.B.6.C** | preview | supporting | `scatter-regression` | A linear function IS fitted, exactly, to readings that suggest a linear association. The readings arrive as a printed table rather than as a SCATTER PLOT, because this unit draws none. |
| **HSS.ID.C.7** | preview | supporting | `scatter-regression` | This claim is supporting, not core. S-ID.C.7 asks the learner to INTERPRET the rate and the constant term in the context of the data. The answer surface here is a number keypad, so the learner returns those two numbers and never says what they mean. Working out the rate and the constant term of the closest line is proved. Interpreting them is not. |
| **HSF.LE.A.1.C** | preview | introduced | `linear-vs-exponential` | Growth and decay by a constant FACTOR are taught and tested, including decay by a unit fraction. A constant PERCENT rate is never stated as a percent anywhere in this unit, so this expectation is only introduced. |
| **HSF.LE.A.1.C** | preview | introduced | `exponential-rule` | Growth and decay by a constant FACTOR are taught and tested, including decay by a unit fraction. A constant PERCENT rate is never stated as a percent anywhere in this unit, so this expectation is only introduced. |

## 4. Process and practice standards

A process standard is how a learner works, not what a unit teaches, so it is the same sentence in Level 1 and in Level 5 and it belongs to neither. The wording and the sentence beside it live once, for the course, in [`standards/practices-algebra1.json`](standards/practices-algebra1.json). Which lines claim a code is read out of the graphs, and is what the columns below count.

**IN CLASS** marks a code at least one line of the shipped route claims. The `Lines` column counts every unit on disk; `Lines on the route` counts only the units a class receives.

### TEKS process standards — 19 TAC §111.39(c)(1)

| Code | In class? | Units | Lines | Lines on the route | Expectation | What the game actually does about it |
|---|---|---|---|---|---|---|
| **A.1(A)** | **IN CLASS** | l1, l2, l3, l4, l5 | 44 | 12 | apply mathematics to problems arising in everyday life, society, and the workplace; | Every skill carries a contextual form, and the mastery gate cannot be cleared without passing at least one modelling item that walks between a situation and the algebra. |
| **A.1(B)** | **IN CLASS** | l1, l2, l3, l4, l5 | 17 | 7 | use a problem-solving model that incorporates analyzing given information, formulating a plan or strategy, determining a solution, justifying the solution, and evaluating the problem-solving process and the reasonableness of the solution; | The worked echo makes the plan visible as a sequence of justified moves, and fades as competence shows. The learner is not asked to write the plan out, so the `justifying` and `evaluating` stages are modelled and recognised rather than composed. |
| **A.1(C)** | **IN CLASS** | l1, l2, l3, l4, l5 | 11 | 4 | select tools, including real objects, manipulatives, paper and pencil, and technology as appropriate, and techniques, including mental math, estimation, and number sense as appropriate, to solve problems; | The balance beam, the split rectangle and the coordinate grid are working manipulatives, not illustrations. But the rift chooses which one appears — the learner does not select it. The selection half of this process standard is not met and is not claimed. MP.5 is claimed by three lines on the Common Core side, for the using half only, and says the same thing. |
| **A.1(D)** | **IN CLASS** | l1, l2, l3, l4, l5 | 47 | 19 | communicate mathematical ideas, reasoning, and their implications using multiple representations, including symbols, diagrams, graphs, and language as appropriate; | Every node declares the representations its gate demands, and the proving run refuses to close until it has spanned two of them with at least one not symbolic. All notation is strict KaTeX, in the reader's own operator conventions. |
| **A.1(E)** | **IN CLASS** | l1, l2, l4, l5 | 12 | 6 | create and use representations to organize, record, and communicate mathematical ideas; | The modelling forms ask the learner to create the symbolic representation of a situation and are scored on the equation itself. Tables and graphs are supplied for reading rather than created, so the `create` half is met in symbols only. |
| **A.1(F)** | **IN CLASS** | l1, l2, l3, l4, l5 | 28 | 10 | analyze mathematical relationships to connect and communicate mathematical ideas; | Table forms make a rule visible across rows before it is written symbolically; the factoring form asks the learner to see a sum as a product; the special-case form asks what kind of statement is left once the unknown has vanished. |
| **A.1(G)** | **IN CLASS** | l1, l2, l3, l4, l5 | 21 | 9 | display, explain, and justify mathematical ideas and arguments using precise mathematical language in written or oral communication. | The dispute forms put two contradictory chains of reasoning side by side and ask which is sound; every wrong chain is a named misconception rather than a random slip. The learner decides and is shown why, but does not compose the justification in their own words. The written-and-oral half of this process standard is a teacher's job, not the game's, and is not claimed. |

### Common Core Standards for Mathematical Practice

| Code | In class? | Units | Lines | Lines on the route | Expectation | What the game actually does about it |
|---|---|---|---|---|---|---|
| **MP.1** | **IN CLASS** | l1, l2, l4, l5 | 10 | 5 | Make sense of problems and persevere in solving them. | Prerequisite gating means a learner never meets a problem they were not equipped for, so persistence is asked for where it can succeed. |
| **MP.2** | **IN CLASS** | l1, l2, l3, l4, l5 | 25 | 9 | Reason abstractly and quantitatively. | Contextual forms move between a situation and its symbols in both directions. |
| **MP.3** | **IN CLASS** | l1, l2, l3, l4, l5 | 14 | 6 | Construct viable arguments and critique the reasoning of others. | Error-analysis items present two cadets' contradictory chains and ask which is sound; each wrong option is a real, named misconception rather than a random number. |
| **MP.4** | **IN CLASS** | l1, l2, l3, l4, l5 | 15 | 6 | Model with mathematics. | The modelling form asks for the equation that records a situation, before any solving happens. |
| **MP.5** | **IN CLASS** | l2, l5 | 3 | 1 | Use appropriate tools strategically. | The tools are real and the learner works in them: a coordinate grid whose scale is cut to the readings on it, a table of measurements, and an answer surface that holds exact fractions rather than rounded decimals. Only three lines claim this practice, and they claim the USING half. The learner does not SELECT the tool — the question hands it over — so the strategic half is not claimed anywhere in this course. |
| **MP.6** | **IN CLASS** | l1, l2, l3, l4, l5 | 24 | 7 | Attend to precision. | Answers are exact rationals; the game never accepts a rounded value, and negative signs are generated deliberately from difficulty band 3. |
| **MP.7** | **IN CLASS** | l1, l2, l3, l4, l5 | 43 | 15 | Look for and make use of structure. | Factoring items ask the learner to see a sum as a product; the area model shows the distributive property as a split rectangle. |
| **MP.8** | **IN CLASS** | l1, l2, l3, l4, l5 | 26 | 8 | Look for and express regularity in repeated reasoning. | Table forms make a rule visible across rows before it is written symbolically. |

## 5. Where the alignment stops

A unit is judged as much by what it refuses to claim as by what it cites. Everything below is a gap this product declares about itself.

### 5.1 Algebra I expectations this course does not claim at all

11 of the 49 are claimed nowhere on disk, at any depth. A further 22 are claimed ONLY inside a preview unit, so a class on the route does not meet them either. Both lists are below, and every expectation is quoted in full so a reader can price the gap rather than guess at it.

**Claimed nowhere — 11 of 49.**

| Code | 19 TAC | The expectation, word for word |
|---|---|---|
| **A.3(D)** | `19 TAC §111.39(c)(3)(D)` | graph the solution set of linear inequalities in two variables on the coordinate plane; |
| **A.3(E)** | `19 TAC §111.39(c)(3)(E)` | determine the effects on the graph of the parent function f(x) = x when f(x) is replaced by af(x), f(x) + d, f(x - c), f(bx) for specific values of a, b, c, and d; |
| **A.3(G)** | `19 TAC §111.39(c)(3)(G)` | estimate graphically the solutions to systems of two linear equations with two variables in real-world problems; and |
| **A.3(H)** | `19 TAC §111.39(c)(3)(H)` | graph the solution set of systems of two linear inequalities in two variables on the coordinate plane. |
| **A.4(A)** | `19 TAC §111.39(c)(4)(A)` | calculate, using technology, the correlation coefficient between two quantitative variables and interpret this quantity as a measure of the strength of the linear association; |
| **A.4(B)** | `19 TAC §111.39(c)(4)(B)` | compare and contrast association and causation in real-world problems; and |
| **A.7(C)** | `19 TAC §111.39(c)(7)(C)` | determine the effects on the graph of the parent function f(x) = x^2 when f(x) is replaced by af(x), f(x) + d, f(x - c), f(bx) for specific values of a, b, c, and d. |
| **A.8(B)** | `19 TAC §111.39(c)(8)(B)` | write, using technology, quadratic functions that provide a reasonable fit to data to estimate solutions and make predictions for real-world problems. |
| **A.9(A)** | `19 TAC §111.39(c)(9)(A)` | determine the domain and range of exponential functions of the form f(x) = ab^x and represent the domain and range using inequalities; |
| **A.9(D)** | `19 TAC §111.39(c)(9)(D)` | graph exponential functions that model growth and decay and identify key features, including y-intercept and asymptote, in mathematical and real-world problems; and |
| **A.9(E)** | `19 TAC §111.39(c)(9)(E)` | write, using technology, exponential functions that provide a reasonable fit to data and make predictions for real-world problems. |

**On disk, but not on the route — 22 of 49.** These are written and validated; a class is not sent to them. The unit that carries each one is named so a district can see exactly what promoting a preview unit would buy.

| Code | 19 TAC | Depth on disk | Unit | The expectation, word for word |
|---|---|---|---|---|
| **A.2(E)** | `19 TAC §111.39(c)(2)(E)` | core | l5 | write the equation of a line that contains a given point and is parallel to a given line; |
| **A.2(F)** | `19 TAC §111.39(c)(2)(F)` | core | l5 | write the equation of a line that contains a given point and is perpendicular to a given line; |
| **A.2(G)** | `19 TAC §111.39(c)(2)(G)` | supporting | l5 | write an equation of a line that is parallel or perpendicular to the X or Y axis and determine whether the slope of the line is zero or undefined; |
| **A.2(H)** | `19 TAC §111.39(c)(2)(H)` | supporting | l5 | write linear inequalities in two variables given a table of values, a graph, and a verbal description; and |
| **A.2(I)** | `19 TAC §111.39(c)(2)(I)` | supporting | l5 | write systems of two linear equations given a table of values, a graph, and a verbal description. |
| **A.4(C)** | `19 TAC §111.39(c)(4)(C)` | supporting | l5 | write, with and without technology, linear functions that provide a reasonable fit to data to estimate solutions and make predictions for real-world problems. |
| **A.6(A)** | `19 TAC §111.39(c)(6)(A)` | supporting | l4 | determine the domain and range of quadratic functions and represent the domain and range using inequalities; |
| **A.6(B)** | `19 TAC §111.39(c)(6)(B)` | core | l4 | write equations of quadratic functions given the vertex and another point on the graph, write the equation in vertex form (f(x) = a(x - h)^2 + k), and rewrite the equation from vertex form to standard form (f(x) = ax^2 + bx + c); and |
| **A.6(C)** | `19 TAC §111.39(c)(6)(C)` | supporting | l4 | write quadratic functions when given real solutions and graphs of their related equations. |
| **A.7(A)** | `19 TAC §111.39(c)(7)(A)` | supporting | l4 | graph quadratic functions on the coordinate plane and use the graph to identify key attributes, if possible, including x-intercept, y-intercept, zeros, maximum value, minimum values, vertex, and the equation of the axis of symmetry; |
| **A.7(B)** | `19 TAC §111.39(c)(7)(B)` | core | l4 | describe the relationship between the linear factors of quadratic expressions and the zeros of their associated quadratic functions; and |
| **A.8(A)** | `19 TAC §111.39(c)(8)(A)` | core | l4 | solve quadratic equations having real solutions by factoring, taking square roots, completing the square, and applying the quadratic formula; and |
| **A.9(B)** | `19 TAC §111.39(c)(9)(B)` | core | l3, l5 | interpret the meaning of the values of a and b in exponential functions of the form f(x) = ab^x in real-world problems; |
| **A.9(C)** | `19 TAC §111.39(c)(9)(C)` | core | l5 | write exponential functions in the form f(x) = ab^x (where b is a rational number) to describe problems arising from mathematical and real-world situations, including growth and decay; |
| **A.10(C)** | `19 TAC §111.39(c)(10)(C)` | core | l4 | determine the quotient of a polynomial of degree one and polynomial of degree two when divided by a polynomial of degree one and polynomial of degree two when the degree of the divisor does not exceed the degree of the dividend; |
| **A.10(E)** | `19 TAC §111.39(c)(10)(E)` | core | l4 | factor, if possible, trinomials with real factors in the form ax^2 + bx + c, including perfect square trinomials of degree two; and |
| **A.10(F)** | `19 TAC §111.39(c)(10)(F)` | supporting | l4 | decide if a binomial can be written as the difference of two squares and, if possible, use the structure of a difference of two squares to rewrite the binomial. |
| **A.11(A)** | `19 TAC §111.39(c)(11)(A)` | core | l4, l5 | simplify numerical radical expressions involving square roots; and |
| **A.11(B)** | `19 TAC §111.39(c)(11)(B)` | core | l3, l5 | simplify numeric and algebraic expressions using the laws of exponents, including integral and rational exponents. |
| **A.12(A)** | `19 TAC §111.39(c)(12)(A)` | core | l5 | decide whether relations represented verbally, tabularly, graphically, and symbolically define a function; |
| **A.12(C)** | `19 TAC §111.39(c)(12)(C)` | core | l5 | identify terms of arithmetic and geometric sequences when the sequences are given in function form using recursive processes; |
| **A.12(D)** | `19 TAC §111.39(c)(12)(D)` | core | l5 | write a formula for the nth term of arithmetic and geometric sequences, given the value of several of their terms; and |

**The shape of what is missing.** Every one of them needs something this product does not yet have: a coordinate plane a learner can shade a half-plane on (`A.3(D)`, `A.3(H)`), a graph a learner can transform and read back (`A.3(E)`, `A.7(C)`, `A.9(D)`), a graphical estimate rather than an exact answer (`A.3(G)`), or a statistics tool — a correlation coefficient, a technology-produced fit — that no answer surface in this game offers (`A.4(A)`, `A.4(B)`, `A.8(B)`, `A.9(E)`). `A.9(A)` needs a domain and range stated as inequalities, which is a written answer and not a number. None of them is blocked by the knowledge graph; all of them are blocked by the answer surfaces the engine can verify independently, and this product ships no item it cannot re-derive.

### 5.2 Declared gaps inside the units

Codes a reader might reasonably expect to find on a unit and will not, in each unit's own words.

| Unit | In class? | Code | 19 TAC | Why not |
|---|---|---|---|---|
| `l3` | preview | `A.12(A)` | `19 TAC §111.39(c)(12)(A)` | "Decide whether relations represented verbally, tabularly, graphically, and symbolically define a function" is a yes/no judgement about a set of pairs. Nothing in the independent checker reads a set of pairs, so no item of that shape could be re-derived, and this unit ships no item it cannot re-derive. The idea is taught — every node states that a rule gives exactly one output for each input — but teaching is not evidence and the code is left uncited. |
| `l3` | preview | `A.10(E)` | `19 TAC §111.39(c)(10)(E)` | Factoring a trinomial is not taught in this unit. The factoring node takes out a common factor and stops there, which is A.10(D) run backwards and not A.10(E). |
| `l3` | preview | `A.10(F)` | `19 TAC §111.39(c)(10)(F)` | Deciding whether a binomial is a difference of two squares, and rewriting it, is not taught. The multiply node produces such products incidentally; recognising one in reverse is a different expectation and is not tested. |
| `l3` | preview | `A.9(A)` | `19 TAC §111.39(c)(9)(A)` | The domain and range of an exponential rule, represented using inequalities, is never asked for. This unit evaluates exponential rules; it does not describe their domain or range, and it writes no inequalities at all. |
| `l3` | preview | `A.9(C)` | `19 TAC §111.39(c)(9)(C)` | Writing an exponential function from a described situation is not asked for. Every exponential rule in this unit is printed and read; none is constructed by the learner. |
| `l3` | preview | `HSF.LE.A.2` | — | Constructing linear and exponential functions from a graph, a description or two input-output pairs. Level 2's write-linear node claims the linear half; the exponential half is not taught here, because this unit reads exponential rules rather than building them. |
| `l3` | preview | `HSF.IF.C.7.E` | — | Graphing exponential functions and showing intercepts and end behaviour. This unit draws no graph of any kind. |
| `l4` | preview | `A.7(C)` | `19 TAC §111.39(c)(7)(C)` | Deciding the effect on the graph of the parent rule when it is replaced by a f(x), f(x) + d, f(x - c) or f(bx). Transformations are not taught in this unit, and the expectation is about a graph, which this product does not draw for a squared rule. |
| `l4` | preview | `A.8(B)` | `19 TAC §111.39(c)(8)(B)` | Writing, using technology, quadratic functions that provide a reasonable fit to data. This unit uses an exact least-squares parabola as a CHECKER, on readings that lie exactly on a squared rule. Calling that a reasonable fit to data would be an over-claim: no item asks the learner to fit anything, and no reading is off the rule. |
| `l4` | preview | `HSN.RN.B.3` | — | "Explain why the sum or product of two rational numbers is rational..." The whole expectation is an explanation, and no independent checker can re-derive an argument. The facts it states are enforced by every item in the radical nodes — a whole number never merges into a radical, and the checker refuses an answer that says it does — but enforcing a fact is not asking for the proof, and this unit does not claim a standard on the strength of the half it can check. |
| `l4` | preview | `HSN.RN.A.2` | — | "Rewrite expressions involving radicals and rational exponents using the properties of exponents." This unit prints no rational exponent at all. A fractional count above a letter is notation that nothing below Level 4 defines, and no term is used before it is defined; the radical half of the expectation on its own is not the expectation. |
| `l4` | preview | `HSF.BF.B.3` | — | Identifying the effect on the graph of replacing f(x) by f(x) + k, k f(x), f(kx) and f(x + k). Refused for the same reason as TEKS A.7(C): transformations are not taught here, and the expectation is about a graph this product cannot draw for a squared rule. |
| `l4` | preview | `HSA.REI.D.10` | — | "Understand that the graph of an equation in two variables is the set of all its solutions plotted in the coordinate plane, often forming a curve." Level 2 claims this for a straight line, which it draws. This unit draws no curve, so the clause the code is most about here is exactly the clause that is missing. |
| `l4` | preview | `HSF.IF.C.9` | — | "Compare properties of two functions each represented in a different way." A comparison needs two rules on one card and one checker that reads both. `verify()` takes one check descriptor per item, so the answer to such an item would rest on the generator rather than on a re-derivation. |
| `l4` | preview | `HSF.IF.C.7.B` | — | "Graph square root, cube root, and piecewise-defined functions..." This unit teaches square roots of numbers and never of a letter: `src/learn/parser.js` refuses a letter under a radical, so no such rule could be re-derived from what a learner is shown, and the renderer could not draw its arc either. |
| `l5` | preview | `A.4(A)` | `19 TAC §111.39(c)(4)(A)` | "Calculate, using technology, the correlation coefficient between two quantitative variables and interpret this quantity as a measure of the strength of the linear association." The checker for this is exact and honest — it never takes a square root, and works instead with r squared and with the sign of r — but each of its three questions needs something the answer surface cannot carry. Two of them need a yes/no or strong/weak VERDICT, and the item builder can localise only two verdict words, so a third would reach a Spanish or Polish learner in English. The third needs a rounded decimal, and the keypad has no decimal point, so the value could not be entered. The idea is taught — the direction of an association is the sign of the exactly fitted rate, and the node says a steep line is not a strong one — but teaching is not evidence, and this unit ships no item it cannot re-derive AND cannot serve. |
| `l5` | preview | `HSS.ID.C.8` | — | "Compute (using technology) and interpret the correlation coefficient of a linear fit." The same reason as A.4(A), in the other framework: the quantity is computable exactly, and no question about it can be asked on this answer surface in three languages. |
| `l5` | preview | `A.8(B)` | `19 TAC §111.39(c)(8)(B)` | "Write, using technology, quadratic functions that provide a reasonable fit to data to estimate solutions and make predictions for real-world problems." Texas files residual analysis under a QUADRATIC fit, and this unit fits no quadratic: the residual node fits straight lines only, and the quadratic strand is another unit's work. The Common Core home of residual analysis, HSS.ID.B.6.B, is claimed instead, at supporting, and its caveat names the plotting clause as the half that is missing. |
| `l5` | preview | `A.3(G)` | `19 TAC §111.39(c)(3)(G)` | "Estimate graphically the solutions to systems of two linear equations with two variables in real-world problems; and" Both halves fail here at once. Nothing in this unit is drawn, so nothing can be estimated GRAPHICALLY; and nothing in this unit is estimated at all, because every answer is exact and every checker refuses a decimal. A.3(F), which shares the graphing half, is claimed at supporting for its second clause only, and says so. |
| `l5` | preview | `A.9(A)` | `19 TAC §111.39(c)(9)(A)` | "Determine the domain and range of exponential functions of the form f(x) = ab^x and represent the domain and range using inequalities;" This unit evaluates and writes exponential rules; it never states the set of inputs a rule accepts or the set of outputs it gives, and it writes no inequality about either. Level 3 declared the same gap for the same reason and it has not closed. |
| `l5` | preview | `A.3(E)` | `19 TAC §111.39(c)(3)(E)` | "Determine the effects on the graph of the parent function f(x) = x when f(x) is replaced by af(x), f(x) + d, f(x - c), f(bx) for specific values of a, b, c, and d;" Every word of this expectation is about a GRAPH, and this unit draws none. Writing the same line three ways is a change of spelling, not a transformation of a trace, and calling it one would be exactly the kind of row that makes a vendor alignment sheet stop being true. |
| `l5` | preview | `HSN.Q.A.1` | — | "Use units as a way to understand problems and to guide the solution of multi-step problems; choose and interpret units consistently in formulas; choose and interpret the scale and the origin in graphs and data displays." The last clause needs a data display with a scale and an origin, and this unit builds none. The rest of the expectation is about carrying units through a calculation, which no item here requires, so the claim would be decorative and it is left off. |

A gap declared by a unit no class receives is a smaller thing than the same gap on the route. The second column says which is which; a reader pricing this product today should read the **ON ROUTE** rows first. On this tree there are none: every row above belongs to a unit a class is not sent to. What the route DOES leave uncovered is section 1.1 and section 5.1 — the 33 Algebra I expectations it does not cite at all — and not this table.

### 5.3 Where the evidence is counted at the line, not at the expectation

The teacher record counts, per expectation, how many of the item forms that carry it this learner has actually met. Level 1 names those forms per expectation, in [`standards/ccss-algebra1-l1.json`](standards/ccss-algebra1-l1.json) and [`standards/teks-algebra1-l1.json`](standards/teks-algebra1-l1.json), which is what lets the record report a line that is proved while an expectation it carries has never once been asked. **Levels 2 to 5 do not name them yet.** Their evidence is counted at the line — every question type the citing skills ask — so on those units a proved line and a covered expectation cannot come apart, and the record cannot report that they have. Inventing a per-expectation list for them would be exactly the vendor alignment sheet this document exists not to be.

Every coverage row of the exported record carries a `basis` field saying which of the two it rests on (`named` or `line`), so a reader of the JSON can tell them apart per expectation. **The printed sheet on screen does not show it yet** — the word for it has to be added to all three locale bundles first, and those belong to the localisation area, not to this one. Until then this paragraph is where the difference is declared.

### 5.4 Common Core is reported without a denominator, on purpose

On the route this course cites 39 Common Core content standards, 21 of them at `core`; across every unit on disk, 72 and 37. No percentage is given for either, and one should be treated with suspicion if it appears elsewhere. Common Core has no "Algebra I" list to be a share of: the content of this course sits across the High School Algebra, Functions, Number and Quantity and Statistics conceptual categories and across the grade 6, 7 and 8 Expressions and Equations and Functions domains, and which of those a district calls "Algebra I" is a district decision. TEKS has a legal list of forty-nine, so TEKS gets a percentage.

### 5.5 The preview units, and what is holding them

3 finished units sit on disk at status `preview`. They are not a work in progress and they are not hidden: `npm run check:courses` generates, verifies and aligns every one of them on every build, in three languages, and `npm run check:mastery` runs them. What holds them is one measured number.

Two readings, one lattice apart:

```bash
# THE SHIPPED ROUTE. Re-measured by this build; see section 7.1.
node tools/simulate.mjs 300 3600 --units algebra1-l1,algebra1-l2
  -> 97.3% true mastery IN ONE UNBROKEN SITTING — the cram, which is the arm both of these readings are on

# THE SAME ROUTE WITH THE NEXT UNIT COMPOSED ONTO IT.
node tools/simulate.mjs 300 3600 --units algebra1-l1,algebra1-l2,algebra1-l3
  -> 64.0% true mastery; a knower proves the whole lattice 2.3% of the time
```

**Both of those are the CRAM arm**, which is the only arm the decision to hold Level 3 was ever measured on. They are comparable with each other and with the first row of section 7.1, and with nothing else here. Whether composing Level 3 would hold up on the shape a class plays has not been measured at all: `npm run check:mastery:full` takes that reading and, on this tree, it has never been run — see section 7.4.

**Where each of those two numbers comes from, because they are not the same kind of number.** The first is re-taken by `node tools/build-mastery-runs.mjs`, recorded in `content/standards/mastery-runs.json` with a fingerprint of the tree it was measured on, and printed in section 7.1 of this document. **The second is quoted from the header of `content/courses.json`**, where the decision to hold the preview units was written down. This build does not re-take it — a second composed Monte-Carlo run on every build is an expense that buys nothing while the decision stands — so it is a dated reading by the person who made the decision, and it is marked as one here rather than dressed up as this build's own.

Composing `algebra1-l3` onto the route takes simulated true mastery, on the cram, from 97.3% to 64.0%, which misses the 80% bar this product is held to by 16.0 points. So it is not put in front of anybody. That is a product decision written into the manifest rather than a defect: `route.units` is the single list the loader, the world, the session and the narrative all read, and adding a unit id to it is the whole change. `npm run check:mastery` reads severity off that same list, so a unit promoted onto the route becomes a hard failure on the next build with no edit to any tool.

**The honest reading of that.** The content for five units exists and is validated. The ADAPTIVE SEQUENCE that gets a learner through five units does not yet, and this document will not call content coverage a course. A district buying this today is buying 2 units and 24 skills, with the rest visible on disk and dated.

## 6. Where the evidence lives, and what a district cannot do with it yet

**There is no account, no roster and no server.** Every figure in a learner's record — every question answered, every claim granted, every claim withdrawn — lives in one browser's `localStorage`, on one device. Nothing is uploaded. There is no sign-in, no class list the software knows about, and no copy of anything anywhere else.

That is a deliberate shape, and it has a real benefit: a school can switch this on with no student data leaving the device and no data-protection review to finish first. It also has a cost, and the cost is the thing a district must price before September, not in September.

| What a district will want to do | What happens today |
|---|---|
| Attach evidence to a named student | The name is typed into the record by whoever is at the keyboard. Nothing verifies it. |
| Find a student's record next week | Only on the same browser profile on the same device. Clearing site data clears the record. |
| Use a shared cart or lab Chromebook | **This is the failure case.** Shared devices and shared profiles mean one store for many students; a signed-out or wiped Chromebook loses the evidence entirely. On the devices this product targets, a district cannot reliably attach evidence to a student at all. |
| Assemble a class record | Each student exports a JSON file from their own screen and hands it over. The teacher drops those files into the CLASS tab and gets a roster. It is assembled by hand, every time. |
| Put marks in a gradebook | The record exports CSV, one row per line of the graph. The import is manual. |
| Audit a claim later | The printed sheet is dated and carries a record id and the item counts behind each claim. It is a document, not a database row; nothing outside the device can confirm it. |

**The record says this about itself, on the paper.** A department head who never opens this file still reads it: the teacher record prints a WHERE THIS RECORD LIVES block, in English, Spanish and Polish, on the screen and on the printed sheet — `src/report/teacher.js` `custodyNote()`. A limitation that lives only in a repository is a limitation a school finds out about in September.

**This is not a defect report; it is a scope statement.** Nothing above is broken. The product does exactly what it was built to do, and what it was built to do does not include being a system of record. A district reading this should assume that TODAY, evidence of mastery is a printed or exported file a student produced, and plan for it as paper.

### 6.1 The smallest honest path to a class record

Written down so the decision can be made with the facts rather than discovered on a Tuesday in September. In increasing order of cost, and none of these is built:

| Step | What it buys | What it costs |
|---|---|---|
| **1. Export / import, which exists** | A teacher can assemble a class today, from files students hand over. | Nothing — it ships. It is manual and it is per-sitting. |
| **2. A signed export** | The exported file carries a signature over its own contents, so a record a teacher receives can be shown not to have been edited after it left the device. | A key in the build and a verify step in the CLASS tab. No server, no account. This is the step that turns a file into evidence. |
| **3. A teacher-side merge** | Two files from the same learner — two sittings, two devices — combine into one record instead of replacing each other, so evidence survives a device change. | A merge rule over the evidence ledger, and a rule for what happens when the two disagree. Still no server. |
| **4. A roster file** | The teacher supplies a class list once; records bind to a name from that list rather than to whatever was typed. | A file format and an identity check. Still no server. |
| **5. A server** | Everything above, automatically, on shared hardware. | An account system, a data-protection review, and a different product. **This is a product decision and is not made here.** |

Steps 2 to 4 are worth stating together because they are the whole of what a district actually asks for — *"can I trust this file, and does it follow the child?"* — and none of them needs a server or an account. Step 5 is the only one that does, and it is the only one that changes what this product is.

## 7. What has actually been measured

A standards claim is a claim about content. Whether learners reach mastery is a different question and is answered by `tools/simulate.mjs`, which runs synthetic learners through the real mastery engine, the real prerequisite gate and the real item bank while tracking a hidden competence the engine never sees. Every figure below is one run of that command. None of them is typed: `content/standards/mastery-runs.json` is generated by `node tools/build-mastery-runs.mjs`, which parses the run's own output, and every row carries a fingerprint of the graph, the item pack, the engine and the simulator it was measured against. `npm run check:standards` re-computes those fingerprints and turns the build red if any figure here was measured on a different tree, so this table cannot go stale quietly. It did once, on a unit that is on the route.

### 7.1 The route, composed — three cadences of one experiment

**One number was published here, and it was about a cohort nobody is.** This section used to print a single figure — 97.3% — under the heading "the figure that is about a class", with the gate marked as passed. That figure is the ONE UNBROKEN SITTING arm: everything the learner knows was learned in the last eight hours and not one re-probe crossed a night. `tools/simulate.mjs` calls that row *the cram* in its own output. On the same tree, `npm run check:mastery` exited 1 over the arm a class actually plays. A document that asserts a figure passes a gate the build refuses is worse than no document, so the pipeline was fixed rather than the sentence: `tools/build-mastery-runs.mjs` now reads the arms through `readArms()` and `verdictOf()` in `tools/simulate-all.mjs` — **the gate's own reader, imported and not copied** — and all three cadences are published below.

| Cadence | The shape this product ships? | True mastery at the end | A week later | A month later | Lowest ability quintile | Any hollow claim | Gate |
|---|---|---|---|---|---|---|---|
| **ONE UNBROKEN SITTING (the cram)** | no | 97.3% | 0.0% | 0.0% | 86.7% | 0.0% | no gate is judged on this row |
| **ACROSS DAYS, every day (24 h)** | **yes** | **90.7%** | 90.3% | 87.7% | 65.0% | 7.7% | passes at 80% |
| **ACROSS DAYS, every third day (72 h)** | **yes** | **5.3%** | 5.0% | 4.0% | 0.0% | 15.7% | **fails at 80%** — **this is the row the gate reads** |

* **ONE UNBROKEN SITTING (the cram)** — one unbroken sitting — everything learned in the last eight hours, no re-probe across a night
* **ACROSS DAYS, every day (24 h)** — 22-minute sittings, saved and reloaded, one night between them
* **ACROSS DAYS, every third day (72 h)** — 22-minute sittings, saved and reloaded, three nights between them — an ordinary school pattern

300 learners, a 3600-item budget, algebra1-l1 + algebra1-l2, 7 of 7 invariants pass, measured 2026-08-29. **The 80% promise is judged on 5.3%** — the WEAKER of the two delivered cadences, because a class that meets three times a week is the same product at a different gap and quoting the friendlier row would be choosing the arm again. It **fails at 80%**.

`npm run check:mastery` refuses this tree, on this:

* **ACROSS DAYS, every third day (72 h)** reads 5.3% true mastery, against a bar of 80%.

```bash
node tools/simulate.mjs 300 3600 --units algebra1-l1,algebra1-l2
npm run check:mastery                     # the same run, with the same reader, as a gate
```

### 7.1.1 What the three rows say, which is the product's whole thesis

**The cram evaporates and the schedule does not.** Same lattice, same bank, same 3600-item budget, same learners — the only difference is the shape the work arrives in. One unbroken sitting reaches 97.3% at the buzzer and **0.0% a week later**. Twenty-two-minute sittings a night apart reach 90.7%, and are still at **87.7% a MONTH later**. That is the case for this product, measured rather than asserted, and it is the reason the cram figure is printed here at all: it is the control, not the claim.

**And the row that does not flatter us, published as it stands.** At a three-day gap the same cohort reaches 5.3%, with 0.0% of the weakest fifth and 15.7% of learners holding a hollow claim. Three days is an ordinary school pattern. It is the difference between this product working and this product not existing, and it is why `npm run check:mastery` is red on this tree.

**And it is a band, not a point.** This row has been recorded 2 times on this repo, between **5.3% and 6.3%**, over trees that differed by prose and by a caveat. The engine, the lattice, the bank and the budget were the same in all of them; the seed was not. Quote the band. A reader who argues with the decimal of this row is arguing with the seed, and the finding is in the first digit.

| Reading | Tree | Every day (24 h) | Every third day (72 h) |
|---|---|---|---|
| 2026-08-28 | `046299fb18974b74` | 90.7% | 6.3% |
| **2026-08-29 — the reading above** | `695e8304610f5e19` | 90.7% | 5.3% |

**What that 5.3% rests on, in writing.** Its ABSOLUTE level is decided by two constants in `tools/simulate.mjs` — `GAP_POW` and `PERMA`, the permastore floor and the forgetting exponent. Power-law forgetting and a Bahrick permastore are both real, **but nobody in this repo has calibrated either constant against a human**, and the run's own 9-cell sweep of them moves this row from **0% to 42%** (the every-day row moves 68% to 98% across the same grid). The engine is identical in all of them; only the forgetting model moves. So the RANKING of the three cadences is a finding of this model and the LEVEL of the three-day row is not yet a measurement of anything. Read the spread before quoting the row. The sweep is printed by the run itself, under *how much of that is the forgetting model?*.

**What `content/courses.json` publishes, and why it is a different number.** The manifest's own header records 98.0% for this route and 64.0% for the route with Level 3 composed onto it. Both of those are **ONE UNBROKEN SITTING (the cram)** readings — the arm above that is not the delivery shape — taken when the decision to hold Level 3 was written down. They are comparable with each other and with the first row of the table above, and with nothing else in this document.

This is the lattice a learner with no query string walks, with cross-unit prerequisites intact. It is not the same measurement as a unit run on its own, and the difference is not small: `--unit algebra1-l2` prunes the Level 1 prerequisites and models a learner who walked into Level 2 with nothing behind them, a condition `src/content/route.js` makes impossible. `npm run check:mastery` re-runs the composed experiment on every build and exits 1 when either delivered cadence is below 80%.

### 7.2 Each unit on its own

Every unit, including the preview ones, run standalone. A preview unit that misses the bar is printed here with the number that misses it.

Each row is the same three cadences as section 7.1. The gate column reads the weaker of the two delivered ones; the cram column is the control and no gate is judged on it.

| Unit | In class? | Learners | Item budget | Every day (24 h) | Every third day (72 h) | The cram, for contrast | Lowest quintile, every day | Any hollow claim, every day | Invariants | Gate | Run on |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `l1` | **ON ROUTE** | 200 | 900 | 93.0% | 66.0% | 100.0% | 87.5% | 5.0% | 7 of 7 pass | **fails at 80%** | 2026-08-29 |
| `l2` | **ON ROUTE** | 200 | 900 | 27.0% | 1.5% | 92.0% | 0.0% | 61.0% | 7 of 7 pass | **fails at 80%** | 2026-08-29 |
| `l3` | preview | 200 | 900 | 79.0% | 39.0% | 99.0% | 47.5% | 14.5% | 7 of 7 pass | **fails at 80%** | 2026-08-29 |
| `l4` | preview | 200 | 900 | 0.5% | 0.0% | 57.0% | 0.0% | 76.0% | 7 of 7 pass | **fails at 80%** | 2026-08-29 |
| `l5` | preview | 200 | 900 | 15.5% | 0.0% | 92.0% | 0.0% | 79.0% | 7 of 7 pass | **fails at 80%** | 2026-08-29 |


```bash
node tools/simulate.mjs --unit algebra1-l5 200 900
node tools/build-mastery-runs.mjs        # re-take every reading in this section
```

**And the per-unit rows are ADVISORY, for two structural reasons.** First, `--unit <id>` prunes cross-unit prerequisites, so it models a learner walking into Level 2 with no Level 1 behind them — the one condition `src/content/route.js` makes impossible. Second, and this is what makes the two across-days columns here so much harsher than section 7.1: the delivered arm spends the item budget in short sittings, so a run at a 900-item budget gets about a quarter of the sittings a run at 3600 does, and every night in between still costs the learner forgetting. A short course delivered across days is the hardest shape in this whole table, and it is not the shape a class is in. **Section 7.1 is the lattice a class walks**, and it is the row `npm run check:mastery` can turn the build red on. These rows say what each unit does when it is asked to stand on nothing, on a quarter of the schooling.

**Read the mastery and the quintile columns together.** The headline figure that has circulated for this product — true mastery near 100% — is Level 1's, on the cram, and Level 1 is ten skills of one-variable linear equations. The weakest unit on disk is `algebra1-l4` at 0.0% on the cadence its gate reads, with 0.0% of the weakest fifth and 76.0% of learners holding a hollow claim when played every day. That is not a defect in the reporting; it is what that unit currently is, and it is printed here because a coverage document that prints only the flattering half of its own evidence is the thing this file exists not to be.

The 80% bar is applied to BOTH delivered cadences, and on this tree 5 units (`l1`, `l2`, `l3`, `l4`, `l5`) miss it standalone. **2 of those are ON THE SHIPPED ROUTE** — and the paragraph above is how to read that: section 7.1 measures those same units COMPOSED, at four times the budget, which is what a class receives, and that run clears the bar on the daily cadence. `tools/simulate.mjs` applies its own, different bar — to the cram — and its exit code is recorded in `content/standards/mastery-runs.json` as `cramGate` beside the gate above, never as it.

**What the figures are not.** They are simulated learners, not children. The model is deliberately ungenerous — a third of learners carry a misconception that resists teaching, an inattention slip applies whatever the learner knows, an unfamiliar representation costs real accuracy, and everything decays with elapsed time — but it is still a model, and the invariants beside each row are the part that is not: they are assertions about the engine, checked on every item and every claim of every run, and zero is the only passing value. **Simulated mastery is not evidence about children, and section 6 is why this product cannot yet produce that evidence at class scale.**

### 7.3 What the mastery gate proves, and one leg of it that is weaker than it reads

Every `core` claim in this document means the same thing: the mastery gate demanded three unassisted items at difficulty band 4 or above, with all support switched off. Three further conditions sit on top of that, and a reader should know how each one is decided, because they are not decided the same way.

| The condition | How it is decided | What that is worth |
|---|---|---|
| **Three unassisted items at band 4+** | counted, item by item, against the measured difficulty of the bank | a measurement |
| **A form or a situation this learner has never practised** | the engine tracks which forms and which situations this learner has actually met | a measurement |
| **Spanned two representations** | the run's items carried two different `rep` TAGS | **a tag, not a measurement** — see below |
| **Included a modelling item** | one of the run's items carried the `rep` tag `context` or `verbal` | **a tag, not a measurement** — see below |

**The last two legs are declared by the item form, not measured from what the learner did.** A `rep` tag is written on the generator, and two forms of one skill can carry two different tags while asking for the same act. On this route that is not hypothetical: `rule-from-table` has four forms tagged `table`, `table`, `context` and `verbal`, and every one of them asks the learner to fill one missing cell of a linear table. A proving run over three of them satisfies both legs, and the learner has done one act three times. `write-linear`'s `context` form prints a sentence with no numbers in it beside the same two coordinate pairs its `symbolic` form uses, so the modelling leg there is a sentence, not a translation.

**What that does and does not undermine.** It does not touch the item count, the difficulty band, the unassisted requirement or the novelty requirement — those four are measured, and they are the load-bearing ones. It means the two transfer legs are currently a claim about the BANK's labelling rather than about the learner. The printed teacher record says so on the paper, in all three languages, rather than reporting "spanned two representations" as a demonstrated fact (`src/report/teacher.js`). The fix on the engine side is a measure of whether two forms are the same act — the same answer type over the same drawn quantity — rather than whether they carry two labels, and it belongs in `src/learn/mastery.js`; it is not made here, and this paragraph is here so that nobody has to find it by reading the generators.

### 7.4 What has not been measured on this tree

A section headed *what has actually been measured* is only honest if it also says what has not. Some of this product's gates cost minutes of real play each and are run per wave rather than per commit; `npm run wave` records every one of them in `progress/gate-runs.json` with its exit code and a content fingerprint of the tree it was measured on. The table below is that ledger, read at the moment this document was generated. It is not a summary of it and it is not typed.

**4 of the gates a learner meets on the shipped route are recorded RED on this exact tree, and 6 more have never been run on it at all.**

| Gate | What its red would mean | State on this tree | Last run |
|---|---|---|---|
| `check:compose` | **a defect a class meets today** | **RED — it refused something** | 2026-08-29, 688 s |
| `check:sustain` | **a defect a class meets today** | **RED — it refused something** | 2026-08-29, 929 s |
| `check:touch` | **a defect a class meets today** | **RED — it refused something** | 2026-08-29, 636 s |
| `check:traffic` | **a defect a class meets today** | **RED — it refused something** | 2026-08-29, 1123 s |
| `check:density` | **a defect a class meets today** | **NEVER RUN** — no evidence either way | — |
| `check:mastery:full` | **a defect a class meets today** | **NEVER RUN** — no evidence either way | — |
| `check:progress` | **a defect a class meets today** | **NEVER RUN** — no evidence either way | — |
| `check:takeover` | **a defect a class meets today** | **NEVER RUN** — no evidence either way | — |
| `check:transient` | **a defect a class meets today** | **NEVER RUN** — no evidence either way | — |
| `check:truncate` | **a defect a class meets today** | **NEVER RUN** — no evidence either way | — |
| `check:layout` | **a defect a class meets today** | passed, on this tree | 2026-08-29, 1729 s |
| `check:motion` | **a defect a class meets today** | passed, on this tree | 2026-08-29, 1139 s |
| `check:figures:render` | a defect in any unit, route or preview | **NEVER RUN** — no evidence either way | — |
| `check:quality` | not unit-scoped | **RED — it refused something** | 2026-08-28, under a second |

**6 route-scope gates have never been run on this tree.** They are `check:mastery:full`, `check:takeover`, `check:progress`, `check:transient`, `check:density`, `check:truncate`. Nothing anywhere says whether this tree passes them; the ledger simply has no row.

**And this is the surface the product is aimed at.** BRIEF.md's definition of done says it works on a school Chromebook and on a phone. Three gates measure that, and here is what the ledger says about each of them on this tree:

* `check:touch` — every on-screen control tapped for real, in three locales and three orientations. **RED — it refused something**.
* `check:layout` — the layout across eight viewports, three locales and the notch rotations. **passed, on this tree**.
* `check:density` — the opening ninety seconds, on a phone held up, a phone on its side and a laptop. **NEVER RUN — no evidence either way**.

Not one of those three is evidence that this tree works on the hardware it is for.

**And the 4 recorded reds are on the route.** `check:compose`, `check:traffic`, `check:sustain`, `check:touch` each refused this tree, with a fingerprint that matches it. A recorded red on a route-scope gate fails `npm run check`; it is printed here because a district reading a standards document should not have to run the build to find out.

`npm run wave` re-takes every row of this table. A row that reads *passed, on a tree that is not this one* is not a failure and is not evidence: the gate passed, and then somebody changed a byte of the product.

## 8. How to check any of this yourself

```bash
node tools/build-standards.mjs --check      # this document has not drifted from the graphs
node tools/build-mastery-runs.mjs --check   # section 7 was measured on THIS tree
node tools/critic/reachable.mjs             # a player with no query string reaches the route in section 1.1
node tools/validate-courses.mjs             # every unit generates, verifies and aligns in three languages
node tools/validate-items.mjs               # Level 1 items re-derived by an independent solver
node tools/check-record.mjs                 # the teacher record reports the truth, and prints whole, on every unit
node tools/check-i18n.mjs                   # no untranslated prose of ours, in src/ or content/
node tools/simulate-all.mjs                 # THE 80% PROMISE, on the composed shipped route
node tools/simulate.mjs --unit <id>         # does the sequence get synthetic learners there
node tools/check-docs.mjs                   # no hole on any page: no missing value, no empty citation
node tools/check-named-cases.mjs            # no claim whose named cases its own items never produce
```

`tools/validate-courses.mjs` fails the build if a node cites a standard with no quoted text, at a depth outside `core`/`supporting`/`introduced`, below `core` with no caveat naming the missing part, or — for TEKS — with no 19 TAC citation. `tools/check-record.mjs` fails if a unit prints another unit's standards, if any line of the printed record ships with an empty standards column, if a code reaches the screen at an unknown depth, if the record is headed with the wrong unit, or if any of it falls off the paper in any of the three languages. This document is generated from the same graphs those two read, so it cannot say something they do not.

And seven things `node tools/build-standards.mjs` refuses to write a document over, rather than writing one and hoping somebody notices:

* a unit whose name in a graph is not its name in the English bundle, or whose Spanish or Polish name is a different shape from the English one. The unit in section 2 was called "Lean, Rate and Pair" by its graph and by every document here, and "Structure and Rate" by the three language bundles a learner reads. Two critics marked it and it survived both, because no gate compared a graph with a bundle. One does now.
* a mastery figure in section 7 measured against a different graph, item pack, engine or simulator than the tree now holds. Those figures went stale once, by thirteen points, on a unit that is on the shipped route, and this document asserted in the present tense that the unit failed its own gate.
* a route figure that has quietly stopped being one. Every "on the route" column comes from the same reader as the on-disk column, filtered by unit; if that filter ever stops filtering, the two columns become one number under two headings and nothing in the output would look wrong. So no code may appear in a route-scoped count unless every unit named beside it is on the route.
* an arithmetic claim in section 1.2 that does not close. The gap between the route figure and the on-disk figure is printed as a sum, and the sum is checked against the two counts it claims to explain.
* a standard the inventory and the graph quote differently, or two units quoting one code two ways.
* an expectation whose word-for-word text is in neither source. The inventory carries the wording of the expectations this course cites NOWHERE, and the graph carries the wording of every one it cites; section 5.1's second table lists exactly the cited ones and read the inventory, which has none for them. TWENTY-TWO CONSECUTIVE ROWS of it printed the word JavaScript prints for a value that is not there — in the column headed *The expectation, word for word*, three lines under the sentence promising that every expectation is quoted in full. Every gate was green, because they all read the sources and the sources were right. There is no fallback now: a field this document cannot fill stops the build.
* a hole anywhere on the finished page. Three rules run over the rendered document before it is written, and again over the copy on disk by `npm run check:docs`. (1) No value may be a JavaScript accident — the four words that language prints for a missing value, or a template placeholder that never expanded. (2) No citation column may hold an empty cell: write an em dash and a reason, because a blank reads as "nothing needs citing here", which is a claim. (3) No coverage figure may appear without saying whether it is what a class receives or what is on disk. The one honest use of that first word in this document is inside 19 TAC §111.39(c)(2)(G), where it describes the slope of an upright line, and it is written down as a named exception with a reason rather than guessed at.
